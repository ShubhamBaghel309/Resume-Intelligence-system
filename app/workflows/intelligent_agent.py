# app/workflows/intelligent_agent.py
from typing import TypedDict, Annotated, Literal, Any
from langgraph.graph import StateGraph, END
from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.output_parsers import JsonOutputParser
from pydantic import BaseModel, Field
from functools import lru_cache
import os
import asyncio
import shutil
import json
import re  

# MCP communication is now handled by app/mcp_infra/executor.py
# Add new MCP servers in MCP/mcp_config.json - no changes needed here

# Import existing components
import sys

sys.path.append(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from querying.hybrid_search import HybridResumeSearch 
from querying.jd_resume_matcher import rank_resumes_for_jd
from generation.answer_generation import generate_answer
import sqlite3


# ============= State Definition =============
class AgentState(TypedDict):
    """State that flows through the agent graph"""

    query: str  # User's original question
    query_analysis: dict  # LLM's analysis of the query
    search_strategy: str  # Chosen strategy: "sql", "vector", "hybrid"
    sql_filters: dict  # Extracted SQL filters
    vector_query: str  # Reformulated query for vector search
    candidate_ids: list  # Resume IDs from SQL filtering
    dropped_filters: list  # Filters that were dropped to relax constraints
    search_results: list  # Raw search results
    final_results: list  # Deduplicated and enriched results
    answer: str  # Generated natural language answer
    should_retry: bool  # Whether to retry with different strategy
    retry_count: int  # Number of retries attempted
    use_llm_sql: bool  # Flag to use LLM-generated SQL on retry
    llm_generated_sql: str  # SQL query generated by LLM
    aggregation_result: float  # Result from aggregation queries (COUNT, AVG, etc.)
    chat_history: list[dict[str, any]]
    conversation_context: dict  # Candidates from previous conversation (for Q&A queries)
    tool_action: dict   # Populated by MCPRegistry when query matches a registered MCP server
    tool_executed: bool  # Whether the MCP tool was successfully executed
    selected_jd: dict  # Resolved JD details for JD-resume matching flow
    jd_match_results: list  # Ranked resume results for JD matching flow
    jd_info: dict  # Formatted JD information for JD info queries


# ============= Query Analysis Models =============
class EntitiesModel(BaseModel):
    """Structured entities extracted from the query"""
    names: list[str] = Field(default_factory=list, description="Candidate names mentioned")
    skills: list[str] = Field(default_factory=list, description="Technical skills mentioned")
    companies: list[str] = Field(default_factory=list, description="Company names mentioned")
    locations: list[str] = Field(default_factory=list, description="Locations mentioned")
    degrees: list[str] = Field(default_factory=list, description="Educational degrees mentioned")
    job_titles: list[str] = Field(default_factory=list, description="Job titles mentioned")
    phone: str | None = Field(default=None, description="Phone number mentioned in query")
    email: str | None = Field(default=None, description="Email address mentioned in query")

class FiltersModel(BaseModel):
    """Structured filters extracted from the query"""
    candidate_ids: list[str] = Field(default_factory=list, description="Specific candidate IDs for follow-up queries")
    min_experience: float | None = Field(default=None, description="Minimum years of experience")
    max_experience: float | None = Field(default=None, description="Maximum years of experience")
    required_skills: list[str] = Field(default_factory=list, description="Required skills")
    location: str | None = Field(default=None, description="Location filter")
    company: str | None = Field(default=None, description="Company filter")
    job_title: str | None = Field(default=None, description="Job title filter")
    current_role: str | None = Field(default=None, description="Current role filter")
    phone: str | None = Field(default=None, description="Filter by phone number")
    email: str | None = Field(default=None, description="Filter by email address")
    institute: str | None = Field(default=None, description="Filter by educational institute/university")
    degree: str | None = Field(default=None, description="Filter by degree type (B.Tech, MBA, etc.)")
    project_keyword: str | None = Field(default=None, description="Filter by project name or technology")

class QueryAnalysis(BaseModel):
    """Structured output from query analyzer"""

    query_type: Literal[
        "name_based",
        "skill_based",
        "experience_based",
        "project_based",
        "education_based",
        "location_based",
        "contact_based",
        "complex_multi_criteria",
        "email_action",
        "jd_match",
        "jd_info",
    ] = Field(description="Primary type of the query")

    intent: str = Field(
        description="What the user wants to know (e.g., 'find candidates', 'get education details', 'compare candidates')"
    )

    entities: EntitiesModel = Field(
        default_factory=EntitiesModel,
        description="Extracted entities: names, skills, companies, locations, degrees, etc."
    )

    filters: FiltersModel = Field(
        default_factory=FiltersModel,
        description="Structured filters: min_experience, max_experience, etc."
    )

    search_strategy: Literal["sql_only", "vector_first", "hybrid"] = Field(
        description="Recommended search strategy based on query analysis"
    )

    confidence: float = Field(
        description="Confidence in the analysis (0.0 to 1.0)", ge=0.0, le=1.0
    )

    reasoning: str = Field(
        description="Brief explanation of why this strategy was chosen"
    )
    is_refinement: bool = Field(
    description="True if this query refines/filters previous results, False if it's a new search",
    default=False
    )
    
    use_llm_sql: bool = Field(
        description="True if query requires LLM-generated SQL (complex logic, aggregations, counts, comparisons), False if hardcoded filters suffice",
        default=False
    )
    
    sql_complexity_reason: str = Field(
        description="Brief explanation of why LLM SQL is or isn't needed for this query",
        default=""
    )
    
    is_aggregation_query: bool = Field(
        description="True if query asks for COUNT, AVG, SUM, MAX, MIN or other aggregations (should return number/stats, not candidates)",
        default=False
    )
    
    is_qa_query: bool = Field(
        description="True if query asks to analyze/explain/suggest based on previous context WITHOUT needing new search (e.g., 'suggest questions based on his resume', 'explain his projects', 'why is he suitable')",
        default=False
    )
    
    is_ambiguous: bool = Field(
        description="True if the query is ambiguous, assumes facts that might not be in the DB, or lacks enough context to accurately query.",
        default=False
    )
    
    clarification_needed: str = Field(
        description="If is_ambiguous is True, this should contain the specific question to ask the user to clarify their intent or assumed facts.",
        default=""
    )

    is_jd_match_query: bool = Field(
        description="True if the query asks to match/rank resumes against a job description",
        default=False,
    )

    is_jd_info_query: bool = Field(
        description="True if the query asks about the job description itself (requirements, skills, details) without matching resumes",
        default=False,
    )

    requested_jd_id: str | None = Field(
        description="Optional jd_id explicitly mentioned in query",
        default=None,
    )


# ============= Complete Database Schema for LLM SQL Generation =============
DATABASE_SCHEMA = """
═══════════════════════════════════════════════════════════════════════════
COMPLETE DATABASE SCHEMA - Resume Intelligence System
═══════════════════════════════════════════════════════════════════════════

TABLE: parsed_resumes
─────────────────────────────────────────────────────────────────────────────
Primary table containing all parsed resume information.

COLUMNS:
├── resume_id (TEXT, PRIMARY KEY)
│   └── Unique identifier for each resume
│
├── document_id (TEXT, FOREIGN KEY → documents.document_id)
│   └── Links to the original uploaded document
│
├── BASIC INFORMATION:
│   ├── candidate_name (TEXT)
│   │   └── Full name of the candidate
│   │   └── Examples: "RATISH NAIR", "Shubham Baghel", "LAVANYA JAIN"
│   │
│   ├── email (TEXT)
│   │   └── Email address
│   │   └── Examples: "ratish.nair@example.com", "john.doe@gmail.com"
│   │
│   ├── phone (TEXT)
│   │   └── Phone number (may include country code, formatting varies)
│   │   └── Examples: "+91-8374106843", "9876543210", "(555) 123-4567"
│   │
│   └── location (TEXT)
│       └── Current location/city
│       └── Examples: "Bangalore", "New Delhi", "Mumbai, Maharashtra"
│
├── PROFESSIONAL SUMMARY:
│   ├── total_experience_years (REAL)
│   │   └── Total years of work experience (numeric, can be decimal)
│   │   └── Examples: 5.0, 3.5, 10.2, 0.0 (freshers)
│   │
│   └── current_role (TEXT)
│       └── Current job title/role
│       └── Examples: "Senior Software Engineer", "Data Scientist", "ML Engineer"
│
├── SKILLS (JSON stored as TEXT):
│   └── skills (TEXT)
│       └── JSON array of all technical and non-technical skills
│       └── Format: ["Python", "Java", "Machine Learning", "AWS", "React"]
│       └── Search using: skills LIKE '%Python%'
│
├── WORK EXPERIENCE (JSON stored as TEXT):
│   └── work_experience (TEXT)
│       └── JSON array of job objects
│       └── Format: [
│             {
│               "company": "Google",
│               "role": "Software Engineer",
│               "duration": "Jan 2020 - Present",
│               "responsibilities": ["Built ML pipelines", "Led team of 5"]
│             },
│             ...
│           ]
│       └── Search companies: work_experience LIKE '%Google%'
│       └── Search roles: work_experience LIKE '%Engineer%'
│
├── EDUCATION (JSON stored as TEXT):
│   └── education (TEXT)
│       └── JSON array of degree objects
│       └── Format: [
│             {
│               "degree": "B.Tech",
│               "field": "Computer Science",
│               "institute": "IIT Delhi",
│               "year": "2016-2020",
│               "grade": "8.5 CGPA"
│             },
│             ...
│           ]
│       └── Search institutes: education LIKE '%IIT%' OR education LIKE '%National Institute of Technology%'
│       └── Search degrees: education LIKE '%MBA%' OR education LIKE '%B.Tech%'
│
├── PROJECTS (JSON stored as TEXT):
│   └── projects (TEXT)
│       └── JSON array of project objects
│       └── Format: [
│             {
│               "name": "RAG System for Healthcare",
│               "description": "Built retrieval system...",
│               "technologies": ["Python", "LangChain", "ChromaDB"],
│               "role": "Lead Developer"
│             },
│             ...
│           ]
│       └── Search: projects LIKE '%RAG%' OR projects LIKE '%Healthcare%'
│
├── ACHIEVEMENTS & CERTIFICATIONS (JSON stored as TEXT):
│   └── additional_information (TEXT)
│       └── JSON containing achievements, certifications, awards, publications
│       └── Format: {
│             "achievements": ["Won hackathon 2023", "Published paper in ACM"],
│             "certifications": ["AWS Certified", "Google Cloud Professional"],
│             "languages": ["English", "Hindi"],
│             "hobbies": ["Photography", "Chess"]
│           }
│       └── Search: additional_information LIKE '%AWS Certified%'
│
└── METADATA:
    ├── parsed_at (TIMESTAMP)
    │   └── When the resume was parsed
    │
    └── indexed_at (TIMESTAMP)
        └── When the resume was indexed to vector store

═══════════════════════════════════════════════════════════════════════════

TABLE: documents
─────────────────────────────────────────────────────────────────────────────
Stores original uploaded PDF documents and extracted text.

COLUMNS:
├── document_id (TEXT, PRIMARY KEY)
├── batch_id (TEXT, FOREIGN KEY → upload_batches.batch_id)
├── raw_text (TEXT) - Full extracted text from PDF
├── original_filename (TEXT) - Original PDF filename
├── file_path (TEXT) - Path to stored PDF
├── status (TEXT) - Processing status: 'uploaded', 'extracted', 'parsed'
└── created_at (TIMESTAMP) - Upload timestamp

═══════════════════════════════════════════════════════════════════════════

IMPORTANT SEARCH PATTERNS:
─────────────────────────────────────────────────────────────────────────────

1. PHONE NUMBER SEARCH (handles different formats):
   WHERE REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(phone, '-', ''), ' ', ''), '(', ''), ')', ''), '+', '') LIKE '%8374106843%'

2. EDUCATION INSTITUTE SEARCH (flexible matching):
   WHERE education LIKE '%IIT%' OR education LIKE '%Indian Institute of Technology%'

3. SKILLS SEARCH (JSON array search):
   WHERE skills LIKE '%Python%' AND skills LIKE '%AWS%'

4. EXPERIENCE RANGE:
   WHERE total_experience_years BETWEEN 3 AND 5

5. COMPANY SEARCH:
   WHERE work_experience LIKE '%Google%' OR work_experience LIKE '%Microsoft%'

6. MULTI-CRITERIA:
   WHERE skills LIKE '%Python%' 
     AND total_experience_years >= 5 
     AND education LIKE '%IIT%'

7. AGGREGATIONS:
   SELECT COUNT(*) FROM parsed_resumes WHERE skills LIKE '%Python%'
   SELECT AVG(total_experience_years) FROM parsed_resumes WHERE education LIKE '%IIT%'
   SELECT candidate_name, total_experience_years FROM parsed_resumes ORDER BY total_experience_years DESC LIMIT 5

═══════════════════════════════════════════════════════════════════════════
"""


def _extract_requested_jd_id(query: str) -> str | None:
    """Extract explicit jd_id token from query when present."""
    match = re.search(r"\b(?:jd[_\-]?[a-z0-9]+|primary_jd)\b", query.lower())
    if match:
        return match.group(0)
    return None


def _detect_jd_match_intent(query: str) -> bool:
    """Heuristic detector for JD-vs-resume matching intent."""
    q = query.lower()

    disqualifiers = ["send email", "send invite", "invitation", "email to"]
    if any(word in q for word in disqualifiers):
        return False

    jd_context = [
        "job description",
        " jd",
        "jd ",
        " role requirements",
    ]
    match_actions = [
        "match",
        "fit",
        "suitable",
        "relevant resumes",
        "best resumes",
        "rank resumes",
        "screen resumes",
        "against jd",
        "for this role",
    ]

    has_jd_context = any(token in f" {q} " for token in jd_context)
    has_match_action = any(token in q for token in match_actions)
    resume_and_job = ("resume" in q or "candidate" in q) and ("job" in q or "jd" in q)

    return has_match_action and (has_jd_context or resume_and_job)


def _detect_jd_info_intent(query: str) -> bool:
    """Heuristic detector for JD information queries (asking ABOUT the JD, not matching)."""
    q = query.lower()

    # If it's a JD match query, it's not an info query
    if _detect_jd_match_intent(query):
        return False

    # Check for skill audit intent
    skill_audit_phrases = [
        "genuine",
        "inflation",
        "inflated",
        "verify skills",
        "validate skills",
        "audit skills",
        "real skills",
        "fake skills",
        "actually use"
    ]
    if any(phrase in q for phrase in skill_audit_phrases):
        return False

    # JD context keywords
    jd_context = [
        "job description",
        " jd ",
        " jd?",
        "the jd",
        "this jd",
        "role requirements",
        "job requirements",
        "position requirements",
    ]

    # Info action keywords (asking about the JD itself)
    info_actions = [
        "what skills",
        "what are the skills",
        "what does the jd",
        "tell me about",
        "show me the jd",
        "describe the",
        "what are the requirements",
        "requirements for",
        "what is required",
        "what experience",
        "looking for",
        "details about",
        "details of",
        "summarize",
        "summary of",
    ]

    has_jd_context = any(token in f" {q} " or q.startswith(token.strip()) or q.endswith(token.strip()) for token in jd_context)
    has_info_action = any(token in q for token in info_actions)

    # Also check for direct JD questions
    direct_jd_questions = [
        "what does the jd require",
        "what skills does the jd",
        "what is the jd about",
        "tell me about the jd",
        "tell me about the job description",
        "what are the jd requirements",
    ]
    is_direct_question = any(phrase in q for phrase in direct_jd_questions)

    return is_direct_question or (has_jd_context and has_info_action)


def _has_active_jd_context(chat_history: list[dict[str, Any]], conversation_context: dict[str, Any]) -> bool:
    """Return True when recent conversation indicates an active JD context."""
    active_jd = conversation_context.get("active_jd", {}) if isinstance(conversation_context, dict) else {}
    if isinstance(active_jd, dict) and active_jd.get("jd_id"):
        return True

    for msg in reversed(chat_history or []):
        if msg.get("role") != "agent":
            continue

        query_analysis = msg.get("query_analysis", {}) or {}
        if query_analysis.get("is_jd_info_query") or query_analysis.get("is_jd_match_query"):
            return True
        if query_analysis.get("query_type") in {"jd_info", "jd_match"}:
            return True

        content = str(msg.get("content", "")).lower()
        if "job description" in content and "jd id" in content:
            return True

    return False


def _is_jd_followup_query(query: str) -> bool:
    """Detect short follow-up queries that refer to the current JD without repeating 'JD'."""
    q = query.lower().strip()

    jd_followup_phrases = [
        "summarize it",
        "summarize this",
        "jd summarize",
        "1-line summary",
        "one line summary",
        "in one line",
        "give me a 1-line summary",
        "give me one line summary",
        "preferred skills",
        "preferred ones",
        "nice to have",
        "must have skills",
        "must-have skills",
        "required skills",
        "mandatory skills",
        "what is the role",
        "role of the jd",
        "role summary",
        "what does this role",
        "what skills does",
        "backend engineer requirement",
        "backend engineering requirement",
        "is it mostly backend",
        "mostly backend",
        "is it backend",
        "requirements right",
        "requirement right",
        "in the jd",
        "for the jd",
        "of the jd",
        "about the jd",
        "the jd require",
        "the jd mention",
        "the jd have",
        "this jd require",
        "this jd mention",
        "this jd have",
        # Location follow-ups
        "where is it located",
        "where is the role",
        "where is the job",
        "what is the location",
        "what location",
        "which city",
        "which location",
        "is it remote",
        "is it hybrid",
        "is it onsite",
        "is it on-site",
        "work from home",
        # Must-have / nice-to-have bare forms
        "a must have",
        "a must-have",
        "a nice to have",
        "is it must have",
        "is it required",
        "is it mandatory",
        "is it optional",
        "is it preferred",
    ]

    if any(phrase in q for phrase in jd_followup_phrases):
        return True

    if "skills" in q and ("required" in q or "preferred" in q or "must" in q):
        return True

    if "must have" in q or "must-have" in q:
        return True

    if "role" in q and any(token in q for token in ["what", "summary", "responsibilit", "basically"]):
        return True

    # Confirmation patterns: "so it's basically X right?"
    confirmation_words = ["basically", "right?", "right", "so it's", "so its", "is this", "is it", "correct"]
    if any(cw in q for cw in confirmation_words):
        context_words = ["backend", "engineer", "engineering", "requirement", "frontend", "fullstack", "devops", "role", "position"]
        if any(kw in q for kw in context_words):
            return True

    return False


def _is_affirmative_reply(query: str) -> bool:
    """Detect very short affirmative replies like yes/sure/okay for continuation flows."""
    q = (query or "").strip().lower().strip(".?!")
    if not q:
        return False
    affirmative_patterns = [
        r"^yes\b",
        r"^yeah\b",
        r"^yep\b",
        r"^sure\b",
        r"^ok\b",
        r"^okay\b",
        r"^please\b",
        r"^go ahead\b",
        r"^continue\b",
        r"^do it\b",
    ]
    return any(re.match(pattern, q) for pattern in affirmative_patterns)


def _classify_jd_info_subintent(query: str) -> str:
    """Classify JD info query into focused answer intents."""
    q = query.lower()

    preferred_markers = ["preferred", "nice to have", "good to have", "optional skills"]
    required_markers = ["must have", "must-have", "required", "mandatory", "essential"]
    summary_markers = ["summarize", "summary", "brief", "overview"]
    one_line_summary_markers = ["1-line summary", "one line summary", "in one line"]
    role_markers = ["role", "position", "what is this role", "role summary", "responsibilities"]
    confirmation_markers = ["basically", "right?", "right", "so it's", "so its", "is this", "is it", "correct"]
    location_markers = ["where", "located", "location", "city", "remote", "hybrid", "onsite", "on-site", "work from home"]

    # Natural confirmation queries should receive a short, direct answer.
    if (
        ("backend" in q or "requirement" in q or "engineer" in q or "engineering" in q or "frontend" in q or "devops" in q)
        and any(token in q for token in confirmation_markers)
    ):
        return "confirmation"

    if any(token in q for token in one_line_summary_markers):
        return "one_line_summary"

    # Location queries about the JD itself
    if any(token in q for token in location_markers):
        return "location"

    # "is kubernetes a must have?" → asks about a specific skill's classification
    if ("must have" in q or "must-have" in q) and not any(token in q for token in ["what are", "list", "show"]):
        return "skill_check"

    if any(token in q for token in summary_markers):
        return "summary"
    if any(token in q for token in preferred_markers):
        return "preferred_skills"
    if any(token in q for token in required_markers):
        return "required_skills"
    if any(token in q for token in role_markers):
        return "role"

    # Common asks like "what skills does the JD require" should map to must-have skills.
    if "skills" in q and ("require" in q or "need" in q):
        return "required_skills"

    return "full"


def _normalize_text_list(value: Any) -> list[str]:
    """Normalize JSON/list/string values into a clean list of strings."""
    if value is None:
        return []

    if isinstance(value, list):
        return [str(item).strip() for item in value if str(item).strip()]

    if isinstance(value, str):
        parsed = None
        if value.strip().startswith("[") and value.strip().endswith("]"):
            try:
                parsed = json.loads(value)
            except Exception:
                parsed = None
        if isinstance(parsed, list):
            return [str(item).strip() for item in parsed if str(item).strip()]
        if value.strip():
            return [value.strip()]

    return [str(value).strip()] if str(value).strip() else []


def _get_resumes_db_path() -> str:
    return os.path.join(
        os.path.dirname(os.path.dirname(os.path.dirname(__file__))), "resumes.db"
    )


@lru_cache(maxsize=1)
def _candidate_name_inventory() -> tuple[str, ...]:
    """Load distinct candidate names once for heuristic fallback matching."""
    try:
        conn = sqlite3.connect(_get_resumes_db_path())
        cursor = conn.cursor()
        cursor.execute(
            """
            SELECT DISTINCT candidate_name
            FROM parsed_resumes
            WHERE candidate_name IS NOT NULL AND TRIM(candidate_name) <> ''
            """
        )
        names = [row[0].strip() for row in cursor.fetchall() if row and row[0] and row[0].strip()]
        conn.close()
        return tuple(sorted(set(names), key=lambda item: (-len(item), item.lower())))
    except Exception:
        return tuple()


def _extract_names_from_query_fallback(query: str) -> list[str]:
    """Best-effort name extraction when LLM analysis is unavailable."""
    query_text = (query or "").strip()
    query_lower = query_text.lower()
    matches: list[str] = []

    for candidate_name in _candidate_name_inventory():
        candidate_lower = candidate_name.lower()
        if len(candidate_lower) < 4:
            continue
        if candidate_lower in query_lower:
            matches.append(candidate_name)

    if matches:
        return list(dict.fromkeys(matches))

    regex_patterns = [
        r"([A-Z][A-Za-z]+(?:\s+[A-Z][A-Za-z]+){1,2})'s",
        r"(?:about|show|what did|tell me about|summary of|experience of|work of|projects of|role of)\s+([A-Z][A-Za-z]+(?:\s+[A-Z][A-Za-z]+){1,2})",
    ]
    banned_suffixes = {"google", "microsoft", "amazon", "python", "java"}
    for pattern in regex_patterns:
        for match in re.finditer(pattern, query_text):
            candidate = " ".join(match.group(1).split())
            if candidate and candidate.lower() not in banned_suffixes:
                matches.append(candidate)

    return list(dict.fromkeys(matches))


def _extract_company_from_query_fallback(query: str) -> str | None:
    """Extract company names from common phrases like 'at Google'."""
    query_text = query or ""
    company_patterns = [
        r"\bat\s+([A-Z][A-Za-z0-9&.,\- ]{1,60})",
        r"\bfrom\s+([A-Z][A-Za-z0-9&.,\- ]{1,60})",
    ]
    split_pattern = r"(?:\?|\.|,|\bwith\b|\bwho\b|\bthat\b|\bhaving\b|\bfor\b|\band\b)"

    for pattern in company_patterns:
        match = re.search(pattern, query_text)
        if not match:
            continue
        raw_value = match.group(1).strip()
        cleaned = re.split(split_pattern, raw_value, maxsplit=1)[0].strip(" .,'\"")
        if cleaned:
            return cleaned
    return None


def _extract_min_experience_fallback(query: str) -> float | None:
    """Extract simple experience constraints such as '5+ years'."""
    query_lower = (query or "").lower()
    patterns = [
        r"(\d+(?:\.\d+)?)\s*\+\s*(?:years?|yrs?)",
        r"more than\s+(\d+(?:\.\d+)?)\s*(?:years?|yrs?)",
        r"(\d+(?:\.\d+)?)\s*(?:or more)\s*(?:years?|yrs?)",
        r"(\d+(?:\.\d+)?)\s*(?:years?|yrs?)\s+of\s+experience",
    ]
    for pattern in patterns:
        match = re.search(pattern, query_lower)
        if match:
            try:
                return float(match.group(1))
            except ValueError:
                return None
    return None


def _extract_job_title_fallback(query: str) -> str | None:
    """Detect job-title hints needed for contradiction checks."""
    query_lower = (query or "").lower()
    title_patterns = [
        "junior intern",
        "junior developer",
        "junior engineer",
        "intern",
        "fresher",
        "junior",
        "senior engineer",
        "senior developer",
        "senior",
    ]
    for title in title_patterns:
        if title in query_lower:
            return title
    return None


def _extract_skills_from_query_fallback(query: str) -> list[str]:
    """Extract a small set of common skills for local fallback routing."""
    query_lower = (query or "").lower()
    canonical_skills = [
        ("machine learning", "Machine Learning"),
        ("generative ai", "Generative AI"),
        ("gen ai", "Gen AI"),
        ("python", "Python"),
        ("java", "Java"),
        ("javascript", "JavaScript"),
        ("react", "React"),
        ("aws", "AWS"),
        ("sql", "SQL"),
    ]
    found = [label for token, label in canonical_skills if token in query_lower]
    return list(dict.fromkeys(found))


def _is_context_qa_query(query: str, conversation_context: dict[str, Any]) -> bool:
    """Detect follow-up queries that should use prior candidate context without new search."""
    if not isinstance(conversation_context, dict):
        return False

    if not conversation_context.get("candidate_ids"):
        return False

    query_lower = (query or "").lower()
    context_markers = [
        "their",
        "them",
        "his",
        "her",
        "these",
        "those",
        "actual work experiences",
        "work experiences of",
        "summarize",
        "summary of",
        "projects",
        "education",
        "skills",
        "experience",
        "why is",
    ]
    return any(marker in query_lower for marker in context_markers)


def _build_fallback_query_analysis(state: AgentState) -> dict[str, Any]:
    """Heuristic query analysis used when remote LLM analysis is unavailable."""
    query = state.get("query", "")
    query_lower = query.lower()
    conversation_context = state.get("conversation_context", {}) or {}

    entities = {
        "names": _extract_names_from_query_fallback(query),
        "skills": _extract_skills_from_query_fallback(query),
        "companies": [],
        "locations": [],
        "degrees": [],
        "job_titles": [],
        "phone": None,
        "email": None,
    }
    filters = {
        "candidate_ids": [],
        "min_experience": None,
        "max_experience": None,
        "required_skills": [],
        "location": None,
        "company": None,
        "job_title": None,
        "current_role": None,
        "phone": None,
        "email": None,
        "institute": None,
        "degree": None,
        "project_keyword": None,
    }

    company = _extract_company_from_query_fallback(query)
    if company:
        entities["companies"] = [company]
        filters["company"] = company

    min_experience = _extract_min_experience_fallback(query)
    if min_experience is not None:
        filters["min_experience"] = min_experience

    job_title = _extract_job_title_fallback(query)
    if job_title:
        entities["job_titles"] = [job_title]
        filters["job_title"] = job_title

    if entities["skills"]:
        filters["required_skills"] = entities["skills"]

    is_qa_query = _is_context_qa_query(query, conversation_context)
    if is_qa_query and conversation_context.get("candidate_ids"):
        filters["candidate_ids"] = list(conversation_context.get("candidate_ids", []))

    is_aggregation_query = any(token in query_lower for token in ["how many", "count", "average", "avg", "maximum", "minimum"])

    if entities["names"]:
        query_type = "name_based"
    elif "project" in query_lower:
        query_type = "project_based"
    elif "education" in query_lower or "degree" in query_lower or "college" in query_lower or "university" in query_lower:
        query_type = "education_based"
    elif entities["skills"]:
        query_type = "complex_multi_criteria" if filters["min_experience"] is not None else "skill_based"
    elif filters["min_experience"] is not None:
        query_type = "experience_based"
    else:
        query_type = "complex_multi_criteria"

    structured_constraints = any(
        [
            entities["names"],
            entities["skills"],
            entities["companies"],
            filters["min_experience"] is not None,
            filters["job_title"],
            filters["candidate_ids"],
        ]
    )

    return {
        "query_type": query_type,
        "intent": "answer resume search query using heuristic fallback analysis",
        "entities": entities,
        "filters": filters,
        "search_strategy": "sql_only" if structured_constraints or is_qa_query else "hybrid",
        "confidence": 0.35,
        "reasoning": "Heuristic fallback analysis used because remote query analysis was unavailable.",
        "is_refinement": False,
        "use_llm_sql": False,
        "sql_complexity_reason": "Fallback analysis avoids remote SQL generation.",
        "is_aggregation_query": is_aggregation_query,
        "is_qa_query": is_qa_query,
        "is_ambiguous": False,
        "clarification_needed": "",
        "is_jd_match_query": False,
        "is_jd_info_query": False,
        "requested_jd_id": None,
    }


def _fetch_jd_record(cursor: sqlite3.Cursor, requested_jd_id: str | None) -> dict[str, Any] | None:
    """Resolve JD using explicit jd_id first, then latest active JD fallback."""
    if requested_jd_id:
        cursor.execute(
            "SELECT * FROM job_descriptions WHERE LOWER(jd_id) = LOWER(?) LIMIT 1",
            (requested_jd_id,),
        )
        row = cursor.fetchone()
        if row:
            return dict(row)

    cursor.execute(
        """
        SELECT *
        FROM job_descriptions
        WHERE status IS NULL OR LOWER(status) IN ('open', 'active')
        ORDER BY COALESCE(indexed_at, updated_at, posting_date, '1970-01-01') DESC
        LIMIT 1
        """
    )
    row = cursor.fetchone()
    if row:
        return dict(row)

    cursor.execute(
        """
        SELECT *
        FROM job_descriptions
        ORDER BY COALESCE(indexed_at, updated_at, posting_date, '1970-01-01') DESC
        LIMIT 1
        """
    )
    row = cursor.fetchone()
    return dict(row) if row else None


# ============= Initialize LLM =============
from dotenv import load_dotenv

load_dotenv()
# API key loaded from .env file

llm = ChatOpenAI(
    model="gpt-4o-mini",
    temperature=0.1,  # Low temperature for structured analysis
    max_tokens=4096
)

answer_llm = ChatOpenAI(model="gpt-4o-mini", temperature=0.1, max_tokens=4096)


# ============= Agent Nodes =============


def summarize_old_messages(messages: list, llm) -> str:
    """
    Summarize old chat messages to preserve context while reducing token usage

    Args:
        messages: List of old message dicts to summarize
        llm: The LLM instance to use for summarization

    Returns:
        A concise summary string
    """
    # Format messages for summarization
    conversation_text = ""
    for msg in messages:
        role = msg.get("role", "unknown")
        content = msg.get("content", "")
        conversation_text += f"{role.upper()}: {content}\n\n"

    summarization_prompt = f"""You are an expert conversation summarizer for a resume search system. Your task is to create ultra-concise summaries that preserve ALL critical information.

CRITICAL RULES:
1. Extract and preserve ALL candidate names mentioned (with resume IDs if available)
2. Preserve ALL search criteria: skills, experience years, locations, companies, education
3. Preserve search refinements: "increased experience from 3 to 5 years", "added Python skill"
4. Use abbreviations: "exp" for experience, "yrs" for years, "ML" for Machine Learning
5. Use compact formats: "Python, Java, AWS" not "Python and Java and AWS"
6. Maximum 80 words - be extremely compact
7. Focus on FACTS, not conversational fluff

FEW-SHOT EXAMPLES:

Example 1:
USER: Find Python developers with more than 5 years experience
AGENT: Found 1 candidate: RATISH NAIR (resume_id: ccded4e6-ed27-4924-95b1-c2e312edc612) with 20 years experience, Python skills
USER: Show me their projects
AGENT: Displayed 15 projects for RATISH NAIR including Generative AI, RAG systems, MLOps
USER: What about their education?
AGENT: RATISH NAIR has B.Tech in Electronics & Communication from CUSAT (2000-2004)

SUMMARY:
Searched Python devs >5yrs exp. Found RATISH NAIR (ID: ccded4e6-ed27-4924-95b1-c2e312edc612, 20yrs exp). Discussed: 15 projects (Gen AI, RAG, MLOps), education (B.Tech Electronics CUSAT 2000-2004).

Example 2:
USER: Find candidates with Machine Learning experience in Bangalore
AGENT: Found 3 candidates in Bangalore with ML skills
USER: Show only those with 3+ years
AGENT: Narrowed to 2 candidates: Shubham Baghel, Anshika Chaudhary
USER: What companies did Shubham work at?
AGENT: Shubham worked at: Google, Microsoft, TCS

SUMMARY:
ML candidates in Bangalore, refined to 3+yrs: Shubham Baghel (ex-Google, Microsoft, TCS), Anshika Chaudhary.

Example 3:
USER: Find Java developers
AGENT: Found 8 Java developers
USER: Filter to those with AWS experience
AGENT: 5 candidates have both Java and AWS
USER: Show their current roles
AGENT: Current roles: 2 Senior Engineers, 2 Tech Leads, 1 Architect

SUMMARY:
Java devs (8 total) → refined to Java+AWS (5 candidates) → roles: 2 Sr Engineers, 2 Tech Leads, 1 Architect.

Example 4:
USER: Who has worked at Google?
AGENT: Found 4 candidates: Alex Kumar (resume_123), Priya Shah (resume_456), Raj Patel (resume_789), Sarah Lee (resume_012)
USER: Which ones know React?
AGENT: 2 candidates: Alex Kumar, Priya Shah have React skills
USER: Show Alex's education
AGENT: Alex Kumar: MS Computer Science Stanford 2018, BS MIT 2016

SUMMARY:
Ex-Google employees (4): Alex Kumar (ID: resume_123), Priya Shah (ID: resume_456), Raj Patel (ID: resume_789), Sarah Lee (ID: resume_012). Refined to React users: Alex, Priya. Alex edu: MS CS Stanford 2018, BS MIT 2016.

Now summarize this conversation using the same ultra-concise format:

CONVERSATION:
{conversation_text}

SUMMARY (max 80 words):"""

    try:
        response = llm.invoke(summarization_prompt)
        summary = response.content.strip()
        return summary
    except Exception as e:
        print(f"⚠️  Summarization failed: {e}")
        # Fallback: just list the topics
        return f"Previous conversation covered {len(messages)} messages about candidate search."




        
def analyze_query_node(state: AgentState) -> AgentState:
    """
    Node 1: Analyze user query to understand intent and plan strategy

    This is the BRAIN of the agent - decides what to do based on the question
    """

    # ============= BUILD CHAT CONTEXT =============
    if state["query"]=="Hi" or state["query"]=="Hello" or state["query"]=="Hey" :
        state["query_analysis"] = {
            "query_type": "greeting",
            "intent": "greet the agent",
            "entities": {},
            "filters": {},
            "search_strategy": None,
            "confidence": 1.0,
            "reasoning": "User is greeting, no search needed",
            "is_refinement": False,
            "use_llm_sql": False,
            "sql_complexity_reason": "",
            "is_aggregation_query": False,
            "is_qa_query": False
        }
        return state

    existing_ctx = state.get("conversation_context", {})
    if not isinstance(existing_ctx, dict):
        existing_ctx = {}

    # Recover persisted context from the latest agent turn when the caller
    # only provides session_id and does not manually pass conversation_context.
    if state["chat_history"]:
        for msg in reversed(state["chat_history"]):
            if msg.get("role") != "agent":
                continue
            msg_ctx = msg.get("conversation_context")
            if isinstance(msg_ctx, dict) and msg_ctx:
                recovered_ctx = dict(msg_ctx)
                recovered_ctx.update(existing_ctx)
                state["conversation_context"] = recovered_ctx
                existing_ctx = recovered_ctx
                break

    chat_context = ""
    previous_candidate_ids = []
    most_recent_candidates = []  # ✅ Track MOST RECENT with names AND IDs

    if state["chat_history"]:
        history = state["chat_history"]

        # Extract candidates from chat history (process NEWEST first)
        for msg in reversed(history):
            if msg.get("role") == "agent":
                msg_ids = msg.get("candidate_ids", [])
                msg_names = msg.get("candidate_names", [])
                
                # Track MOST RECENT candidates (from last agent response)
                if not most_recent_candidates and msg_ids:
                    # Pair names with IDs
                    for i, cid in enumerate(msg_ids):
                        name = msg_names[i] if i < len(msg_names) else "Unknown"
                        most_recent_candidates.append({"id": cid, "name": name})
                
                # Collect all candidate IDs
                previous_candidate_ids.extend(msg_ids)

        # Remove duplicates from all IDs
        previous_candidate_ids = list(dict.fromkeys(previous_candidate_ids))

        # Check if we need to summarize (more than 10 messages)
        if len(history) > 10:
            # Split into old (to summarize) and recent (keep full)
            old_messages = history[:-10]  # All except last 10
            recent_history = history[-10:]  # Last 10 messages
            
            print(f"   📚 Summarizing {len(old_messages)} old messages...")
            summary = summarize_old_messages(old_messages, llm)
            
            chat_context = f"CONVERSATION SUMMARY (older messages):\n{summary}\n\n"
            chat_context += "RECENT CONVERSATION (last 10 messages):\n"
            
            # Format recent messages
            for i, msg in enumerate(recent_history, 1):
                role = msg.get("role", "unknown")
                content = msg.get("content", "")
                chat_context += f"{i}. {role.upper()}: {content}\n"
        else:
            # Short conversation, use all messages
            recent_history = history
            chat_context = "CONVERSATION HISTORY:\n"
            
            # Format all messages
            for i, msg in enumerate(recent_history, 1):
                role = msg.get("role", "unknown")
                content = msg.get("content", "")
                chat_context += f"{i}. {role.upper()}: {content}\n"

        # ✅ CRITICAL: Show MOST RECENT candidates with names for "these/those" pronoun resolution
        if most_recent_candidates:
            chat_context += f"\n📌 MOST RECENT CANDIDATES (use these for 'these'/'those'/'them' pronouns):\n"
            for i, cand in enumerate(most_recent_candidates[:10], 1):
                chat_context += f"   {i}. {cand['name']} (ID: {cand['id']})\n"
        
        # Show all discussed candidates if different from most recent
        if len(previous_candidate_ids) > len(most_recent_candidates):
            other_ids = [cid for cid in previous_candidate_ids if cid not in [c['id'] for c in most_recent_candidates]]
            if other_ids:
                chat_context += f"\n📋 OTHER CANDIDATES (earlier in conversation): {', '.join(other_ids[:5])}\n"
    
    # ✅ ALWAYS save conversation context to state for later use
    if most_recent_candidates:
        existing_ctx["candidate_ids"] = [c["id"] for c in most_recent_candidates]
        existing_ctx["candidate_names"] = [c["name"] for c in most_recent_candidates]
        state["conversation_context"] = existing_ctx

    # Handle pending clarification continuation (e.g., user replies "yes")
    pending_clarification = existing_ctx.get("pending_clarification", {}) if isinstance(existing_ctx, dict) else {}
    if pending_clarification and _is_affirmative_reply(state.get("query", "")):
        pending_ids = pending_clarification.get("candidate_ids", [])
        pending_names = pending_clarification.get("candidate_names", [])
        if pending_ids:
            existing_ctx["candidate_ids"] = pending_ids
            existing_ctx["candidate_names"] = pending_names

        if pending_names:
            joined_names = ", ".join(pending_names)
            state["query"] = f"Please provide a concise summary of the actual work experiences of {joined_names}."
        else:
            state["query"] = "Please provide a concise summary of the candidate's actual work experiences."

        existing_ctx.pop("pending_clarification", None)
        state["conversation_context"] = existing_ctx
        print("   ↪️ Interpreted confirmation reply as clarification follow-up request")
    
    # ============= QUERY ANALYSIS PROMPT =============
    analysis_prompt = ChatPromptTemplate.from_messages(
        [
            (
                "system",
                """You are a query analyzer for a recruitment system. Your ONLY job is to extract structured information from user queries.

────────────────────────────────
YOUR TASK
────────────────────────────────
1. Extract entities (names, skills, locations, etc.) from the query
2. Classify the query type (skill_based, education_based, etc.)
3. Choose the search strategy (sql_only, vector_first, hybrid)
4. Decide if LLM-generated SQL is needed (use_llm_sql flag)
5. Detect ambiguity: if the query assumes facts not present or needs clarification, set `is_ambiguous`=true and provide `clarification_needed`.

────────────────────────────────
PRONOUN RESOLUTION
────────────────────────────────
When user uses pronouns (his/her/their/these/those/them):
- Look at 📌 MOST RECENT CANDIDATES section in chat context
- Extract the referenced candidate name(s) to entities.names
- Examples:
  • "Show their education" → extract names from MOST RECENT CANDIDATES
  • "His skills" → extract the single candidate from MOST RECENT CANDIDATES
  • "These 3 candidates" → extract first 3 from MOST RECENT CANDIDATES
"""),
            (
                "user",
                """{chat_context}

CURRENT QUERY: {query}

════════════════════════════════
STEP 1: EXTRACT ENTITIES
════════════════════════════════
Extract ALL explicitly mentioned information:

**Names:** Extract any candidate names mentioned
- Examples: "Lavanya Jain", "John Smith"
- Add to entities.names

**Contact Info:** Extract phone/email
- Phone: "8374106843", "+91-9876543210"
- Email: "john@example.com"
- Add to entities.phone and entities.email

**Companies:** Extract company names
- Add to entities.companies

════════════════════════════════
STEP 2: EXTRACT FILTERS
════════════════════════════════
Extract ALL criteria mentioned in the query:

**Skills:** "X developer" or "with X skill"
- "Python developers" → filters.required_skills = ["Python"]
- Extract X as skill, not job title

**Experience:** "X+ years", "X-Y years"
- "5+ years" → filters.min_experience = 5
- "3-7 years" → filters.min_experience = 3, filters.max_experience = 7

**Location:** City/country mentioned
- "in Bangalore" → filters.location = "Bangalore"

**Education:** Institute names or degrees
- "IIT graduates" → filters.institute = "IIT"
- "MBA candidates" → filters.degree = "MBA"

**Projects/Companies:** Keywords in work history
- "worked at Google" → filters.company = "Google"
- "built chatbot" → filters.project_keyword = "chatbot"

**MULTI-CRITERIA CRITICAL:** Extract ALL filters even if query has multiple types!
- "Python developers from IIT with 5+ years" → extract ALL three filters
- DO NOT skip any criteria

════════════════════════════════
STEP 3: CLASSIFY QUERY TYPE
════════════════════════════════
Choose ONE primary type:
- skill_based: "Python developers", "candidates with AWS"
- education_based: "IIT graduates", "MBA candidates"
- experience_based: "5+ years experience", "worked at Google"
- contact_based: "phone is X", "email is Y"
- project_based: "built X project"
- general: Broad questions without specific filters

════════════════════════════════
STEP 4: CHOOSE SEARCH STRATEGY
════════════════════════════════
**sql_only** - Exact matches using SQL, return ALL results:
- Name/phone/email lookup: "Find John Smith", "phone 8374106843"
- Education lookup: "IIT graduates", "MBA from NIT"
- Multi-criteria: "Python developers with 5+ years in Bangalore"
- Use when: Clear filters, no semantic ranking needed, want ALL matches

**vector_first** - Semantic search ONLY (no SQL filtering):
- Subjective/qualitative keywords: "rare", "unique", "interesting", "innovative",
  "creative", "impressive", "exceptional", "outstanding", "remarkable",
  "groundbreaking", "cutting-edge", "visionary", "standout"
- Abstract human qualities that CANNOT be stored in a database column:
  "leadership", "communication skills", "analytical thinking",
  "problem-solving", "self-starter", "proactive", "growth mindset",
  "entrepreneurial", "cross-functional", "T-shaped", "well-rounded",
  "renaissance", "continuous learning"
- Similarity/profile matching: "Similar to Elon Musk", "startup founder profile"
- Examples: "Most rare projects", "Unique backgrounds", "Strong leadership qualities",
  "People with entrepreneurial backgrounds", "Find renaissance engineers",
  "Find the most impressive resumes", "Show candidates with exceptional backgrounds"
- Returns: Top 10 semantically relevant
- IMPORTANT: Generic nouns like "resumes", "candidates", "profiles", "backgrounds",
  "people", "engineers" are NOT concrete SQL-filterable criteria. Only SPECIFIC VALUES count
  as concrete SQL filters: skill names (Python, Java, TensorFlow), location names (Bangalore,
  Delhi), institute names (IIT, NIT), experience numbers (3+ years), degree names (B.Tech, MBA).
- Use when: Query contains NO concrete SQL-filterable criterion — ONLY subjective/abstract terms

**hybrid** - SQL filter + vector ranking:
- DECISION PROCEDURE — follow these 2 steps:
  Step A: Does the query contain a CONCRETE SQL-FILTERABLE value?
    Concrete = specific skill name, specific location, specific institute, experience number,
    specific degree. Examples: Python, Java, TensorFlow, Docker, NLP, React, Angular, AWS,
    Bangalore, Mumbai, Pune, Hyderabad, IIT, NIT, BITS, B.Tech, MBA, 5+ years, 3 years.
  Step B: Does the query ALSO contain a SUBJECTIVE/QUALITATIVE word?
    Subjective = impressive, creative, innovative, outstanding, remarkable, unique,
    groundbreaking, cutting-edge, visionary, standout, interesting, exceptional, rare.
  → If BOTH Step A and Step B are YES → choose **hybrid**
  → If ONLY Step A is YES (no subjective word) → choose **sql_only**
  → If ONLY Step B is YES (no concrete filter) → choose **vector_first**

- Examples of hybrid queries (BOTH concrete + subjective):
  • "Python developers with creative projects" → SQL: Python, Vector: creative
  • "Java engineers with impressive work experience" → SQL: Java, Vector: impressive
  • "React developers with innovative side projects" → SQL: React, Vector: innovative
  • "IIT graduates with exceptional achievements" → SQL: IIT, Vector: exceptional
  • "ML engineers with cutting-edge research" → SQL: ML, Vector: cutting-edge
  • "TensorFlow experts with groundbreaking research" → SQL: TensorFlow, Vector: groundbreaking
  • "Docker specialists with innovative DevOps solutions" → SQL: Docker, Vector: innovative
  • "NLP engineers with impressive publications" → SQL: NLP, Vector: impressive
  • "Full-stack developers with standout contributions" → SQL: full-stack, Vector: standout
  • "Cloud architects with visionary approaches" → SQL: cloud, Vector: visionary
  • "Data scientists with unique specializations" → SQL: data science, Vector: unique
  • "B.Tech graduates with the most impressive internships" → SQL: B.Tech, Vector: impressive
  • "Backend developers from Pune with outstanding system design" → SQL: backend+Pune, Vector: outstanding
  • "Python and Java developers with creative problem-solving" → SQL: Python+Java, Vector: creative
  • "5+ years candidates with outstanding project work" → SQL: 5+ years, Vector: outstanding
  • "Candidates from Bangalore with remarkable portfolios" → SQL: Bangalore, Vector: remarkable

- CRITICAL RULES:
  1. If ANY subjective word appears ALONGSIDE ANY concrete filter → MUST be hybrid, NEVER sql_only!
  2. If a concrete skill/location/degree exists but you also see qualitative words → hybrid, NEVER vector_first!
  3. "Java engineers with impressive work experience" = hybrid (Java is concrete, impressive is subjective)
  4. "Docker specialists with innovative solutions" = hybrid (Docker is concrete, innovative is subjective)
- SQL filters exact criteria, vector ranks by relevance
- Returns: Top 10 after filtering

════════════════════════════════
STEP 5: SQL COMPLEXITY DECISION
════════════════════════════════
**use_llm_sql = False** (use hardcoded filters):
- Simple lookups: "Find John", "Python developers"
- Basic filters: "5+ years experience", "candidates in Bangalore"
- 2-3 simple criteria: "Python developers in Mumbai with 3+ years"

**use_llm_sql = True** (generate custom SQL):
- AGGREGATIONS: "how many", "count", "average", "total"
  • Set is_aggregation_query=True
  • Example: "How many Python developers?"
  
- RANKINGS: "best", "top", "most", "highest", "lowest", "maximum", "minimum"
  • Example: "Who has the most experience?" → ORDER BY DESC LIMIT 1
  • Example: "Show the most experienced IIT graduate" → ORDER BY DESC LIMIT 1
  • Example: "Top 3 Python developers by experience" → ORDER BY DESC LIMIT 3
  • RULE: ANY superlative ("most experienced", "best", "top N") = use_llm_sql = True
  • WARNING: Always use LLM SQL for rankings! Even single-result rankings!
  
- COMPLEX LOGIC: ANY use of parentheses, OR operators, AND operators, or nested conditions
  • Example: "(Python OR Java) AND (AWS OR Azure OR GCP)"
  • Example: "(ML OR AI OR DL) skills"
  • Example: "(React OR Angular OR Vue) developers"
  • Example: "(Docker OR Kubernetes) AND (AWS OR GCP)"
  • Example: "Python or Java developers" (even without parentheses, OR logic = LLM SQL)
  • RULE: If query contains OR between skills/institutes/locations → use_llm_sql = True
  • RULE: If query contains parentheses with boolean logic → use_llm_sql = True
  • WARNING: Even 2 items joined by OR requires LLM SQL! Do NOT treat "X or Y" as simple.
  • When use_llm_sql=True for complex logic, keep search_strategy=sql_only (NOT hybrid)
    unless there is ALSO a subjective/qualitative word requiring vector ranking.

Set sql_complexity_reason to explain your decision.

════════════════════════════════
STEP 6: EMAIL ACTION DETECTION
════════════════════════════════
**Set query_type = "email_action"** ONLY if query asks to SEND/DELIVER an email or invitation:
- "Send interview email to X"
- "Send invitation to shubham@example.com"
- "Email interview invite to candidate X"
Do NOT set email_action for queries that merely MENTION interviews without sending:
- "Suggest interview questions" → NOT email_action (this is Q&A)
- "Prepare behavioral questions" → NOT email_action (this is Q&A)
- "Recommend assessment areas" → NOT email_action (this is Q&A)

Extract candidate name/email to entities.

════════════════════════════════
STEP 7: Q&A QUERY DETECTION
════════════════════════════════
**Set is_qa_query = True** if query asks to ANALYZE/EXPLAIN/SUGGEST based on previous context WITHOUT needing new database search:

Examples that DON'T need search:
- "Suggest 10 technical questions based on his resume"
- "Explain why he is suitable for this role"
- "What are his strengths and weaknesses?"
- "Summarize his experience"
- "Compare these two candidates"
- "Why should we hire him?"

These queries use data from PREVIOUS conversation context - NO database search needed!
**Set is_qa_query = True and skip all other analysis.**""",
            ),
        ]
    )

    chain = analysis_prompt | llm.with_structured_output(QueryAnalysis)

    try:
        analysis = chain.invoke(
            {
                "query": state["query"],  # ✅ Original query unchanged
                "chat_context": chat_context,  # ✅ Context passed separately
            }
        )

        analysis_data = analysis.model_dump()
        requested_jd_id = _extract_requested_jd_id(state["query"])
        jd_intent = _detect_jd_match_intent(state["query"])
        jd_info_intent = _detect_jd_info_intent(state["query"])

        # Respect model classification when it already identified JD intent.
        if analysis_data.get("query_type") == "jd_match":
            jd_intent = True
        if analysis_data.get("query_type") == "jd_info":
            jd_info_intent = True

        # Resolve implicit JD follow-ups like "preferred skills?" from session context.
        has_active_jd = _has_active_jd_context(
            state.get("chat_history", []),
            state.get("conversation_context", {}),
        )
        if not jd_intent and not jd_info_intent and has_active_jd and _is_jd_followup_query(state["query"]):
            jd_info_intent = True

        if jd_intent:
            analysis_data["query_type"] = "jd_match"
            analysis_data["is_jd_match_query"] = True
            analysis_data["requested_jd_id"] = requested_jd_id
        elif jd_info_intent:
            analysis_data["query_type"] = "jd_info"
            analysis_data["is_jd_info_query"] = True
            analysis_data["requested_jd_id"] = requested_jd_id

        state["query_analysis"] = analysis_data
        state["search_strategy"] = analysis.search_strategy
        state["sql_filters"] = analysis.filters.model_dump()
        state["use_llm_sql"] = analysis.use_llm_sql  # Set flag from analysis
        state["selected_jd"] = {}
        state["jd_match_results"] = []
        state["jd_info"] = {}

        # ── MCP Tool Action Detection (config-driven via MCPRegistry) ──────────────
        # Skip MCP matching when JD intent is already resolved (prevents
        # jd_generator "job description" keyword from hijacking JD info queries).
        state["tool_action"] = {}
        state["tool_executed"] = False

        if not jd_intent and not jd_info_intent:
          try:
            from mcp_infra.registry import MCPRegistry
            registry = MCPRegistry()

            # 1. Config-based keyword matching (primary)
            matched_server = registry.match_intent(state["query"])

            # 2. LLM fallback for backward compat (e.g., query_type == "email_action")
            #    Guard: only trust this if (a) not a Q&A query and (b) query has send/email/invite words
            if not matched_server and analysis.query_type == "email_action" and not analysis.is_qa_query:
                _q_lower = state.get("query", "").lower()
                if any(w in _q_lower for w in ("send", "email", "invite", "invitation")):
                    matched_server = "interview_email"

            # 3. Continuation: pending tool action from previous turn
            conversation_context = state.get("conversation_context", {})
            if not matched_server and conversation_context.get("pending_tool_action"):
                matched_server = conversation_context["pending_tool_action"]["server_id"]
                print(f"   🔧 Continuing pending tool action: {matched_server}")

            if matched_server:
                server_cfg = registry.get_server_config(matched_server)
                state["tool_action"] = {
                    "server_id": matched_server,
                    "needs_candidate_search": server_cfg.get("needs_candidate_search", False)
                }
                print(f"   🔧 Tool action: {server_cfg.get('name', matched_server)}")
          except Exception as _reg_err:
            print(f"   ⚠️  MCPRegistry error: {_reg_err}")

        print(f"\n🧠 QUERY ANALYSIS:")
        print(f"   Type: {analysis.query_type}")
        print(f"   Strategy: {analysis.search_strategy}")
        print(f"   Reasoning: {analysis.reasoning}")
        print(f"   Confidence: {analysis.confidence:.2f}")
        
        # Check if this is a Q&A query that doesn't need search
        if analysis.is_qa_query:
            print(f"   💬 Q&A QUERY: No search needed - will use previous context")
            # conversation_context already set earlier with most_recent_candidates
        
        
        if analysis.use_llm_sql:
            print(f"   🤖 LLM SQL: YES - {analysis.sql_complexity_reason}")
        else:
            print(f"   ⚡ SQL Mode: Hardcoded filters")
        
        # Explain what will happen based on strategy
        if analysis.search_strategy == "sql_only":
            print(f"   📊 Result source: SQL (all matching candidates, no vector limit)")
        elif analysis.search_strategy == "vector_first":
            print(f"   🔍 Result source: Vector search (top 10 semantic matches)")
        elif analysis.search_strategy == "hybrid":
            print(f"   🔀 Result source: SQL + Vector (top 10 from SQL matches)")
        if chat_context:
            print(
                f"   📜 Used conversation context ({len(state['chat_history'])} messages)"
            )

    except Exception as e:
        print(f"⚠️  Analysis failed: {e}. Falling back to heuristic analysis.")
        analysis_data = _build_fallback_query_analysis(state)
        requested_jd_id = _extract_requested_jd_id(state["query"])
        jd_intent = _detect_jd_match_intent(state["query"])
        jd_info_intent = _detect_jd_info_intent(state["query"])
        has_active_jd = _has_active_jd_context(
            state.get("chat_history", []),
            state.get("conversation_context", {}),
        )
        if not jd_intent and not jd_info_intent and has_active_jd and _is_jd_followup_query(state["query"]):
            jd_info_intent = True

        if jd_intent:
            analysis_data["query_type"] = "jd_match"
            analysis_data["is_jd_match_query"] = True
            analysis_data["requested_jd_id"] = requested_jd_id
        elif jd_info_intent:
            analysis_data["query_type"] = "jd_info"
            analysis_data["is_jd_info_query"] = True
            analysis_data["requested_jd_id"] = requested_jd_id

        state["query_analysis"] = analysis_data
        state["search_strategy"] = analysis_data.get("search_strategy", "sql_only")
        state["sql_filters"] = analysis_data.get("filters", {})
        state["use_llm_sql"] = analysis_data.get("use_llm_sql", False)
        state["selected_jd"] = {}
        state["jd_match_results"] = []
        state["jd_info"] = {}
        state["tool_action"] = {}
        state["tool_executed"] = False

        if not jd_intent and not jd_info_intent:
            try:
                from mcp_infra.registry import MCPRegistry

                registry = MCPRegistry()
                matched_server = registry.match_intent(state["query"])
                conversation_context = state.get("conversation_context", {})
                if not matched_server and conversation_context.get("pending_tool_action"):
                    matched_server = conversation_context["pending_tool_action"]["server_id"]
                    print(f"   🔧 Continuing pending tool action: {matched_server}")

                if matched_server:
                    server_cfg = registry.get_server_config(matched_server)
                    state["tool_action"] = {
                        "server_id": matched_server,
                        "needs_candidate_search": server_cfg.get("needs_candidate_search", False),
                    }
                    print(f"   🔧 Tool action: {server_cfg.get('name', matched_server)}")
            except Exception as _reg_err:
                print(f"   ⚠️  MCPRegistry error during fallback analysis: {_reg_err}")

    return state


def jd_resume_match_node(state: AgentState) -> AgentState:
    """
    Node: Match all resumes against a resolved JD using deterministic scoring.

    Resolution order:
    1. Explicit jd_id in query (if present)
    2. Latest active/open JD
    3. Latest JD overall
    """

    print("\n🎯 EXECUTING JD-RESUME MATCHING...")

    db_path = os.path.join(
        os.path.dirname(os.path.dirname(os.path.dirname(__file__))), "resumes.db"
    )

    try:
        conn = sqlite3.connect(db_path)
        conn.row_factory = sqlite3.Row
        cursor = conn.cursor()

        analysis = state.get("query_analysis", {})
        requested_jd_id = analysis.get("requested_jd_id")

        jd_record = _fetch_jd_record(cursor, requested_jd_id)
        if not jd_record:
            state["selected_jd"] = {}
            state["jd_match_results"] = []
            state["final_results"] = []
            state["candidate_ids"] = []
            state["should_retry"] = False
            state["answer"] = "I couldn't find any indexed job description to match against. Please run JD indexing first."
            conn.close()
            return state

        cursor.execute("SELECT * FROM parsed_resumes")
        resumes = [dict(row) for row in cursor.fetchall()]
        conn.close()

        if not resumes:
            state["selected_jd"] = jd_record
            state["jd_match_results"] = []
            state["final_results"] = []
            state["candidate_ids"] = []
            state["should_retry"] = False
            state["answer"] = "I couldn't find resumes in the database to compare against the JD."
            return state

        ranked = rank_resumes_for_jd(jd_record=jd_record, resumes=resumes, top_k=10)

        state["selected_jd"] = jd_record
        state["jd_match_results"] = ranked
        state["final_results"] = ranked
        state["candidate_ids"] = [item.get("resume_id") for item in ranked if item.get("resume_id")]
        state["should_retry"] = False

        print(f"   ✅ JD resolved: {jd_record.get('job_title', 'Unknown role')} ({jd_record.get('jd_id', 'unknown')})")
        print(f"   ✅ Ranked {len(ranked)} resumes")

    except Exception as err:
        state["selected_jd"] = {}
        state["jd_match_results"] = []
        state["final_results"] = []
        state["candidate_ids"] = []
        state["should_retry"] = False
        state["answer"] = f"JD matching failed: {err}"
        print(f"   ❌ JD matching failed: {err}")

    return state


def jd_info_node(state: AgentState) -> AgentState:
    """
    Node: Fetch and display job description information.

    Handles queries asking ABOUT the JD (requirements, skills, details)
    without matching resumes.
    """

    print("\n📋 FETCHING JD INFORMATION...")

    db_path = os.path.join(
        os.path.dirname(os.path.dirname(os.path.dirname(__file__))), "resumes.db"
    )

    try:
        conn = sqlite3.connect(db_path)
        conn.row_factory = sqlite3.Row
        cursor = conn.cursor()

        analysis = state.get("query_analysis", {})
        conversation_context = state.get("conversation_context", {})
        active_jd = conversation_context.get("active_jd", {}) if isinstance(conversation_context, dict) else {}
        requested_jd_id = analysis.get("requested_jd_id") or active_jd.get("jd_id")

        jd_record = _fetch_jd_record(cursor, requested_jd_id)
        conn.close()

        if not jd_record:
            state["selected_jd"] = {}
            state["jd_match_results"] = []
            state["final_results"] = []
            state["candidate_ids"] = []
            state["should_retry"] = False
            state["answer"] = "I couldn't find any indexed job description. Please run JD indexing first using scripts/JDindexing.py."
            return state

        # Parse skills from JSON if stored as string
        required_skills = jd_record.get("required_skills", "[]")
        nice_to_have_skills = jd_record.get("nice_to_have_skills", "[]")

        try:
            if isinstance(required_skills, str):
                required_skills = json.loads(required_skills)
            if isinstance(nice_to_have_skills, str):
                nice_to_have_skills = json.loads(nice_to_have_skills)
        except json.JSONDecodeError:
            required_skills = [required_skills] if required_skills else []
            nice_to_have_skills = [nice_to_have_skills] if nice_to_have_skills else []

        # Build formatted JD info response
        jd_info = {
            "jd_id": jd_record.get("jd_id", "unknown"),
            "job_title": jd_record.get("job_title", "Unknown Role"),
            "status": jd_record.get("status", "active"),
            "required_skills": required_skills,
            "nice_to_have_skills": nice_to_have_skills,
            "role_summary": jd_record.get("role_summary", ""),
            "responsibilities": jd_record.get("responsibilities", ""),
            "company_overview": jd_record.get("company_overview", ""),
            "location": jd_record.get("location", ""),
            "job_level": jd_record.get("job_level", ""),
        }

        state["selected_jd"] = jd_record
        state["jd_info"] = jd_info
        state["jd_match_results"] = []
        state["final_results"] = []
        state["candidate_ids"] = []
        state["should_retry"] = False

        existing_ctx = state.get("conversation_context", {})
        existing_ctx["active_jd"] = jd_info
        state["conversation_context"] = existing_ctx

        print(f"   ✅ JD found: {jd_info['job_title']} ({jd_info['jd_id']})")

    except Exception as err:
        state["selected_jd"] = {}
        state["jd_info"] = {}
        state["jd_match_results"] = []
        state["final_results"] = []
        state["candidate_ids"] = []
        state["should_retry"] = False
        state["answer"] = f"Failed to fetch JD information: {err}"
        print(f"   ❌ JD info fetch failed: {err}")

    return state


def validate_assumptions_node(state: AgentState) -> AgentState:
    """
    Node: Validates assumptions like entities belonging to the company/role mentioned.
    If entities do not exist or are ambiguous, marks `is_ambiguous = True` so we can ask the user.
    """
    print("\n🔍 VALIDATING ASSUMPTIONS...")
    
    analysis = state.get("query_analysis", {})
    if analysis.get("is_ambiguous"):
        return state  # Already marked ambiguous by LLM

    # We could do a quick DB check here if names and companies are both provided but don't match,
    # or if we want to ensure an entity exists at all.
    # For now, we perform a basic check: if neither candidate IDs, names, skills, nor any other
    # core filter is found for a search intent, we might consider it ambiguous.
    
    # 1. Look for obvious contradictions in the filters:
    filters = analysis.get("filters", {})
    job_title = (filters.get("job_title") or "").lower()
    min_exp = filters.get("min_experience")

    if job_title and min_exp is not None:
        if ("junior" in job_title or "intern" in job_title or "fresher" in job_title) and min_exp >= 5:
            analysis["is_ambiguous"] = True
            analysis["clarification_needed"] = f"You asked for a '{job_title}' but specified {min_exp}+ years of experience. Did you mean a Senior role, or a Junior role with less experience?"
            state["query_analysis"] = analysis
            print(f"   ⚠️ Assumptions invalid: Contradictory role/experience ({job_title}, {min_exp} yrs)")
            return state

    # 2. Check DB for missing entities (Names) and wrong assumed company for a known person
    entities = analysis.get("entities", {})
    names = entities.get("names", [])
    companies = entities.get("companies", [])
    requested_company = (filters.get("company") or (companies[0] if companies else "") or "").strip()

    def _build_name_where_clause(name: str) -> tuple[str, list[str]]:
        """Build a robust name matcher using all name tokens with AND logic."""
        name_parts = [p for p in name.strip().split() if p]
        if not name_parts:
            return "candidate_name LIKE ?", [f"%{name}%"]
        clause = " AND ".join(["candidate_name LIKE ?"] * len(name_parts))
        params_local = [f"%{part}%" for part in name_parts]
        return clause, params_local

    def _extract_company_suggestions(work_experience_text: str) -> list[str]:
        """Extract likely company names from serialized work_experience text."""
        suggestions: list[str] = []
        if not work_experience_text:
            return suggestions

        # Common JSON style: "company": "Samsung"
        json_companies = re.findall(r'"company"\s*:\s*"([^"]+)"', work_experience_text, flags=re.IGNORECASE)
        for comp in json_companies:
            comp_clean = comp.strip()
            if comp_clean:
                suggestions.append(comp_clean)

        # Fallback narrative style: "at <Company Name>"
        text_companies = re.findall(r'\bat\s+([A-Z][A-Za-z0-9&.,\- ]{1,40})', work_experience_text)
        for comp in text_companies:
            comp_clean = comp.strip(" .,-")
            if len(comp_clean) >= 2:
                suggestions.append(comp_clean)

        # De-duplicate while preserving order
        unique: list[str] = []
        seen: set[str] = set()
        for comp in suggestions:
            key = comp.lower()
            if key in seen:
                continue
            seen.add(key)
            unique.append(comp)
        return unique[:4]

    if names:
        db_path = os.path.join(
            os.path.dirname(os.path.dirname(os.path.dirname(__file__))), "resumes.db"
        )
        try:
            conn = sqlite3.connect(db_path)
            cursor = conn.cursor()
            missing_names = []
            for name in names:
                name_clause, name_params = _build_name_where_clause(name)
                cursor.execute(f"SELECT 1 FROM parsed_resumes WHERE {name_clause} LIMIT 1", name_params)
                if not cursor.fetchone():
                    missing_names.append(name)
            conn.close()
            
            if missing_names:
                analysis["is_ambiguous"] = True
                analysis["clarification_needed"] = f"I couldn't find any candidates named {', '.join(missing_names)} in the database. Could you please check the names or provide more context?"
                state["query_analysis"] = analysis
                print(f"   ⚠️ Assumptions invalid: missing names {missing_names}")
                return state

            # Validate assumption: person + company asked, but company not found in person's experience
            if requested_company:
                conn = sqlite3.connect(db_path)
                cursor = conn.cursor()

                matched_rows: list[tuple[str, str, str, str, float | None]] = []
                for name in names:
                    name_clause, name_params = _build_name_where_clause(name)
                    sql = (
                        "SELECT resume_id, candidate_name, work_experience, current_role, total_experience_years "
                        f"FROM parsed_resumes WHERE {name_clause}"
                    )
                    cursor.execute(sql, name_params)
                    matched_rows.extend(cursor.fetchall())

                conn.close()

                requested_company_lower = requested_company.lower()
                company_matches = []
                for row in matched_rows:
                    work_exp = (row[2] or "")
                    current_role = (row[3] or "")
                    haystack = f"{work_exp} {current_role}".lower()
                    if requested_company_lower in haystack:
                        company_matches.append(row)

                if matched_rows and not company_matches:
                    candidate_name = matched_rows[0][1] or names[0]
                    current_role = matched_rows[0][3] or "their current role"
                    total_exp = matched_rows[0][4]

                    alternatives = _extract_company_suggestions(matched_rows[0][2] or "")
                    alt_phrase = ""
                    if alternatives:
                        if len(alternatives) == 1:
                            alt_phrase = f" They appear to have worked at {alternatives[0]}."
                        else:
                            alt_phrase = f" They appear to have worked at: {', '.join(alternatives)}."

                    summary_part = f"{candidate_name} is currently associated with {current_role}"
                    if total_exp is not None:
                        summary_part += f" with about {total_exp} years of experience"
                    summary_part += "."

                    analysis["is_ambiguous"] = True
                    analysis["clarification_needed"] = (
                        f"I couldn't find any record of {candidate_name} working at {requested_company}. "
                        f"{summary_part}{alt_phrase} "
                        f"Would you like me to share a summary of their actual work experiences instead?"
                    )
                    clarification_ids = [row[0] for row in matched_rows if row and row[0]]
                    clarification_names = [row[1] for row in matched_rows if row and row[1]]
                    analysis["clarification_context"] = {
                        "candidate_ids": list(dict.fromkeys(clarification_ids)),
                        "candidate_names": list(dict.fromkeys(clarification_names)),
                    }
                    state["query_analysis"] = analysis
                    print(
                        "   ⚠️ Assumptions invalid: requested company not found for candidate "
                        f"({candidate_name} vs {requested_company})"
                    )
                    return state
        except Exception as err:
            print(f"   ⚠️ Could not validate assumptions against DB: {err}")
            
    print("   ✅ Assumptions validated.")
    return state


def generate_clarification_node(state: AgentState) -> AgentState:
    """
    Node: Uses LLM (or predefined message) to ask a clarifying question.
    """
    print("\n❓ GENERATING CLARIFICATION...")
    analysis = state.get("query_analysis", {})
    clarification = analysis.get("clarification_needed", "Could you please clarify your request? I found it a bit ambiguous.")
    clarification_context = analysis.get("clarification_context", {})

    if clarification_context:
        existing_ctx = state.get("conversation_context", {})
        existing_ctx["pending_clarification"] = clarification_context
        state["conversation_context"] = existing_ctx
    
    # We could also use an LLM here to refine the clarification question
    state["answer"] = clarification
    state["search_results"] = []
    state["final_results"] = []
    state["candidate_ids"] = []
    state["should_retry"] = False
    
    print(f"   ✅ Clarification generated: {clarification}")
    return state


def sql_filter_node(state: AgentState) -> AgentState:
    """
    Node 2: Execute SQL filtering based on extracted entities

    Only runs if strategy requires SQL (sql_only, hybrid)
    Routes to LLM SQL if query analysis indicates complexity
    """

    if state["search_strategy"] == "vector_first":
        print("\n📊 SKIPPING SQL (vector-first strategy)")
        state["candidate_ids"] = []
        return state

    # Check if analysis recommends LLM SQL
    analysis = state.get("query_analysis", {})
    if analysis.get("use_llm_sql", False):
        print("\n📊 ROUTING TO LLM SQL GENERATION...")
        print(f"   Reason: {analysis.get('sql_complexity_reason', 'Complex query detected')}")
        state["use_llm_sql"] = True
        return state

    print("\n📊 EXECUTING HARDCODED SQL FILTERS...")

    filters = state["sql_filters"]
    analysis = state["query_analysis"]
    
    # Check if user wants to filter from previous results ("out of these", "from them", "out of all", etc.)
    query_lower = state["query"].lower()
    context_filter_phrases = [
        "out of these", "from these", "from those", "among these", 
        "among them", "out of them", "from the above", "from previous",
        "out of the", "from the ", "among the ", "within these",
        "in these", "in the above",
        # CRITICAL: "out of all X" phrases that reference previous results
        "out of all the", "out of all those", "from all the", "from all these",
        "among all the", "among all those", "within all the"
    ]
    is_context_filter = any(phrase in query_lower for phrase in context_filter_phrases)
    
    # Get previous candidate IDs from conversation context
    previous_candidate_ids = state.get("conversation_context", {}).get("candidate_ids", [])
    
    if is_context_filter and previous_candidate_ids:
        print(f"   🔗 CONTEXT FILTERING: Restricting search to previous {len(previous_candidate_ids)} candidates")
        # Override candidate_ids to enforce context filtering
        filters["candidate_ids"] = previous_candidate_ids
        state["sql_filters"]["candidate_ids"] = previous_candidate_ids
        
        # CRITICAL: When filtering from context, CLEAR any redundant filters that were already applied
        # For example: "out of these JavaScript developers" - JavaScript was already filtered in previous query
        # We only want to check for NEW criteria (like "machine learning") within the context
        # 
        # Strategy: Keep candidate_ids filter + only NEW criteria mentioned in current query

    # Build SQL query dynamically
    import os

    db_path = os.path.join(
        os.path.dirname(os.path.dirname(os.path.dirname(__file__))), "resumes.db"
    )
    conn = sqlite3.connect(db_path)
    cursor = conn.cursor()

    where_clauses = []
    params = []

    # ✅ PRIORITY: Check if explicit names are mentioned in the query
    # If yes, ignore candidate_ids filter (user wants fresh search for those names)
    entities = analysis.get("entities", {})
    has_explicit_names = bool(entities.get("names"))
    
    # ✅ Candidate IDs filter (for follow-up queries) - ONLY if no explicit names
    if filters.get("candidate_ids") and not has_explicit_names:
        candidate_id_list = filters["candidate_ids"]
        placeholders = ",".join("?" * len(candidate_id_list))
        where_clauses.append(f"resume_id IN ({placeholders})")
        params.extend(candidate_id_list)
        print(
            f"   🎯 Follow-up query: Filtering to {len(candidate_id_list)} previously discussed candidates"
        )

    # Extract names from entities (process names even if candidate_ids was set)
    if has_explicit_names:
        names = entities["names"]
        if len(names) > 1:
            # Multiple names: match ANY of them with OR
            name_or_conditions = []
            for name in names:
                name_parts = name.strip().split()
                if len(name_parts) >= 2:
                    # Multi-part name: match all parts with AND
                    name_and_parts = []
                    for part in name_parts:
                        name_and_parts.append("candidate_name LIKE ?")
                        params.append(f"%{part}%")
                    name_or_conditions.append(f"({' AND '.join(name_and_parts)})")
                else:
                    # Single name part
                    name_or_conditions.append("candidate_name LIKE ?")
                    params.append(f"%{name}%")
            
            where_clauses.append(f"({' OR '.join(name_or_conditions)})")
            print(f"   🎯 Name filter: Matching ANY of {len(names)} names: {', '.join(names)}")
        else:
            # Single name
            name = names[0]
            name_parts = name.strip().split()
            if len(name_parts) >= 2:
                # Multi-part name: match all parts (prevents partial matches)
                name_conditions = []
                for part in name_parts:
                    name_conditions.append("candidate_name LIKE ?")
                    params.append(f"%{part}%")
                where_clauses.append(f"({' AND '.join(name_conditions)})")
                print(f"   🎯 Name filter: Matching all parts of '{name}'")
            else:
                # Single name: use simple LIKE
                where_clauses.append("candidate_name LIKE ?")
                params.append(f"%{name}%")

    # Experience filters
    if filters.get("min_experience"):
        where_clauses.append("total_experience_years >= ?")
        params.append(filters["min_experience"])

    if filters.get("max_experience"):
        where_clauses.append("total_experience_years <= ?")
        params.append(filters["max_experience"])

    # ✅ Compute should_skip_job_filters BEFORE it is used below
    tool_action = state.get("tool_action", {})
    is_tool_action_with_search = bool(tool_action) and tool_action.get("needs_candidate_search", False)
    should_skip_job_filters = is_tool_action_with_search and has_explicit_names

    if should_skip_job_filters:
        print(f"   🔧 Tool action with explicit names: Ignoring job/company filters (used for tool content only)")

    # Location filter
    # Skip if tool action with explicit names (location may refer to interview venue, not candidate)
    if filters.get("location") and not should_skip_job_filters:
        where_clauses.append("location LIKE ?")
        params.append(f"%{filters['location']}%")

    # Skills filter with intelligent expansion for abbreviations and synonyms
    if filters.get("required_skills"):
        # Define skill name expansions (abbreviations, synonyms, related terms)
        skill_expansions = {
            # AI/ML Skills
            "gen ai": ["Gen AI", "Generative AI", "GenAI", "LLM", "GPT", "Large Language Model"],
            "generative ai": ["Gen AI", "Generative AI", "GenAI", "LLM", "GPT"],
            "ml": ["ML", "Machine Learning", "machine learning"],
            "machine learning": ["ML", "Machine Learning", "machine learning"],
            "ai": ["AI", "Artificial Intelligence", "artificial intelligence"],
            "nlp": ["NLP", "Natural Language Processing", "natural language processing"],
            "cv": ["CV", "Computer Vision", "computer vision", "image processing"],
            "deep learning": ["Deep Learning", "DL", "Neural Networks", "deep learning"],
            
            # Programming Languages
            "js": ["JS", "JavaScript", "Javascript", "javascript"],
            "javascript": ["JS", "JavaScript", "Javascript", "javascript"],
            "ts": ["TS", "TypeScript", "Typescript", "typescript"],
            "typescript": ["TS", "TypeScript", "Typescript"],
            "py": ["Python", "python", "py"],
            "python": ["Python", "python"],
            
            # Frameworks/Libraries
            "react": ["React", "ReactJS", "React.js", "react"],
            "reactjs": ["React", "ReactJS", "React.js"],
            "angular": ["Angular", "AngularJS", "Angular.js"],
            "vue": ["Vue", "VueJS", "Vue.js"],
            "node": ["Node", "NodeJS", "Node.js", "node.js"],
            "nodejs": ["Node", "NodeJS", "Node.js"],
            
            # Cloud/DevOps
            "aws": ["AWS", "Amazon Web Services", "amazon web services"],
            "gcp": ["GCP", "Google Cloud Platform", "Google Cloud", "google cloud"],
            "azure": ["Azure", "Microsoft Azure", "azure"],
            "k8s": ["K8s", "Kubernetes", "kubernetes"],
            "kubernetes": ["K8s", "Kubernetes", "kubernetes"],
            "docker": ["Docker", "docker", "containerization"],
            
            # Data/Databases
            "sql": ["SQL", "sql", "MySQL", "PostgreSQL", "database"],
            "nosql": ["NoSQL", "MongoDB", "Cassandra", "DynamoDB"],
            "mongodb": ["MongoDB", "Mongo", "mongo"],
            
            # Other
            "rest": ["REST", "RESTful", "REST API", "restful api"],
            "api": ["API", "REST API", "Web API", "api"],
        }
        
        # CRITICAL: When we have multiple skills, we need to decide AND vs OR logic
        # - If context filtering is active: Use AND (all skills must match within the filtered set)
        # - If no context filtering: Use OR (any skill can match)
        
        skill_groups = []  # Each group is a list of OR conditions for one skill
        
        for skill in filters["required_skills"]:
            skill_lower = skill.lower().strip()
            skill_conditions = []
            
            # Check if skill has known expansions
            if skill_lower in skill_expansions:
                # Add OR conditions for all variations of this ONE skill
                variations = skill_expansions[skill_lower]
                for variation in variations:
                    skill_conditions.append("skills LIKE ?")
                    params.append(f"%{variation}%")
                print(f"   💡 Skill '{skill}' expanded to: {', '.join(variations)}")
            else:
                # No expansion - use as-is
                skill_conditions.append("skills LIKE ?")
                params.append(f"%{skill}%")
            
            # Group the variations for this skill (OR within the group)
            if skill_conditions:
                skill_groups.append(f"({' OR '.join(skill_conditions)})")
        
        # Now combine skill groups based on context
        if skill_groups:
            if is_context_filter and len(skill_groups) > 1:
                # Context filtering with multiple skills: Use AND
                # Example: "out of these JavaScript developers, who has machine learning"
                # → Must have BOTH JavaScript AND Machine Learning
                where_clauses.append(f"({' AND '.join(skill_groups)})")
                print(f"   🔗 Context mode: Requiring ALL {len(skill_groups)} skills (AND logic)")
            else:
                # Normal search or single skill: Use OR
                # Example: "Find JavaScript or Python developers"
                where_clauses.append(f"({' OR '.join(skill_groups)})")
                if len(skill_groups) > 1:
                    print(f"   🔍 Normal mode: Matching ANY of {len(skill_groups)} skills (OR logic)")

    # (should_skip_job_filters was already computed above, before the location filter)

    # Job title filter (searches in work_experience JSON and current_role)
    # Skip if email action with explicit names
    if filters.get("job_title") and not should_skip_job_filters:
        job_title = filters["job_title"]
        # Search in both work_experience JSON and current_role column
        where_clauses.append("(work_experience LIKE ? OR current_role LIKE ?)")
        params.append(f"%{job_title}%")
        params.append(f"%{job_title}%")

    # Company filter (searches in work_experience JSON)
    # Skip if email action with explicit names
    if filters.get("company") and not should_skip_job_filters:
        where_clauses.append("work_experience LIKE ?")
        params.append(f"%{filters['company']}%")

    # Current role filter (exact match in current_role column)
    if filters.get("current_role"):
        where_clauses.append("current_role LIKE ?")
        params.append(f"%{filters['current_role']}%")

    # Phone filter (flexible matching - removes formatting characters)
    if filters.get("phone"):
        phone = filters["phone"]
        # Remove common phone formatting characters for flexible matching
        phone_clean = phone.replace("-", "").replace(" ", "").replace("(", "").replace(")", "").replace("+", "")
        where_clauses.append("REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(phone, '-', ''), ' ', ''), '(', ''), ')', ''), '+', '') LIKE ?")
        params.append(f"%{phone_clean}%")
        print(f"   📞 Phone filter: {phone}")

    # Email filter
    if filters.get("email"):
        where_clauses.append("email LIKE ?")
        params.append(f"%{filters['email']}%")
        print(f"   📧 Email filter: {filters['email']}")

    # Institute filter (searches in education JSON)
    # Smart expansion for common institutes (IIT, NIT, BITS, etc.)
    if filters.get("institute"):
        institute_name = filters["institute"]
        
        # Define common institute abbreviation expansions
        institute_expansions = {
            "IIT": ["IIT", "Indian Institute of Technology"],
            "NIT": ["NIT", "National Institute of Technology"],
            "BITS": ["BITS", "Birla Institute of Technology and Science"],
            "IIIT": ["IIIT", "Indian Institute of Information Technology"],
            "IIM": ["IIM", "Indian Institute of Management"],
            "ISI": ["ISI", "Indian Statistical Institute"],
            "AIIMS": ["AIIMS", "All India Institute of Medical Sciences"],
        }
        
        # Check if the institute matches any known abbreviation (case-insensitive)
        institute_upper = institute_name.upper().strip()
        expanded_names = None
        
        # Exact match or starts with the abbreviation
        for abbrev, expansions in institute_expansions.items():
            if institute_upper == abbrev or institute_upper.startswith(abbrev + " "):
                expanded_names = expansions
                break
        
        if expanded_names:
            # Create OR clause for all variations
            institute_conditions = []
            for variation in expanded_names:
                institute_conditions.append("education LIKE ?")
                params.append(f"%{variation}%")
            where_clauses.append(f"({' OR '.join(institute_conditions)})")
            print(f"   🎓 Institute filter: {institute_name} → Searching for: {', '.join(expanded_names)}")
        else:
            # No expansion needed - use as-is
            where_clauses.append("education LIKE ?")
            params.append(f"%{institute_name}%")
            print(f"   🎓 Institute filter: {institute_name}")

    # Degree filter (searches in education JSON)
    if filters.get("degree"):
        where_clauses.append("education LIKE ?")
        params.append(f"%{filters['degree']}%")
        print(f"   📜 Degree filter: {filters['degree']}")

    # Project keyword filter (searches in projects JSON)
    if filters.get("project_keyword"):
        where_clauses.append("projects LIKE ?")
        params.append(f"%{filters['project_keyword']}%")
        print(f"   💼 Project filter: {filters['project_keyword']}")

    # Build final query
    sql = "SELECT resume_id FROM parsed_resumes"
    if where_clauses:
        sql += " WHERE " + " AND ".join(where_clauses)

    print(f"   SQL: {sql}")
    print(f"   Params: {params}")

    cursor.execute(sql, params)
    results = cursor.fetchall()

    # --- GRACEFUL FILTER RELAXATION ---
    dropped_filters = []
    if not results and where_clauses:
        print("   ⚠️ Zero results found with strict SQL filters. Attempting graceful filter relaxation...")
        
        # Define optional filters that might over-constrain the query
        filters_to_try_dropping = ["location", "company", "institute", "degree", "max_experience", "min_experience", "project_keyword", "job_title", "current_role"]
        
        # Identify which constraints are actually present in the query
        active_filters = {k: v for k, v in filters.items() if v}
        for filter_key in filters_to_try_dropping:
            if filter_key in active_filters:
                dropped_filters.append(filter_key)
                print(f"   🔄 Relaxing filter constraint: '{filter_key}'")

        if dropped_filters:
            # Rebuild query using ONLY names, candidate_ids, and skills
            relaxed_clauses = []
            relaxed_params = []

            # 1. Candidate IDs
            if filters.get("candidate_ids") and not has_explicit_names:
                candidate_id_list = filters["candidate_ids"]
                placeholders = ",".join("?" * len(candidate_id_list))
                relaxed_clauses.append(f"resume_id IN ({placeholders})")
                relaxed_params.extend(candidate_id_list)

            # 2. Names
            if has_explicit_names:
                names = entities["names"]
                if len(names) > 1:
                    name_or_conditions = []
                    for name in names:
                        name_parts = name.strip().split()
                        if len(name_parts) >= 2:
                            name_and_parts = ["candidate_name LIKE ?"] * len(name_parts)
                            for part in name_parts:
                                relaxed_params.append(f"%{part}%")
                            name_or_conditions.append(f"({' AND '.join(name_and_parts)})")
                        else:
                            name_or_conditions.append("candidate_name LIKE ?")
                            relaxed_params.append(f"%{name}%")
                    relaxed_clauses.append(f"({' OR '.join(name_or_conditions)})")
                else:
                    name = names[0]
                    name_parts = name.strip().split()
                    if len(name_parts) >= 2:
                        name_conditions = ["candidate_name LIKE ?"] * len(name_parts)
                        for part in name_parts:
                            relaxed_params.append(f"%{part}%")
                        relaxed_clauses.append(f"({' AND '.join(name_conditions)})")
                    else:
                        relaxed_clauses.append("candidate_name LIKE ?")
                        relaxed_params.append(f"%{name}%")

            # 3. Skills
            if filters.get("required_skills"):
                skill_groups_relaxed = []
                for skill in filters["required_skills"]:
                    skill_lower = skill.lower().strip()
                    skill_conditions = []
                    if skill_lower in skill_expansions:
                        variations = skill_expansions[skill_lower]
                        for variation in variations:
                            skill_conditions.append("skills LIKE ?")
                            relaxed_params.append(f"%{variation}%")
                    else:
                        skill_conditions.append("skills LIKE ?")
                        relaxed_params.append(f"%{skill}%")
                    if skill_conditions:
                        skill_groups_relaxed.append(f"({' OR '.join(skill_conditions)})")

                if skill_groups_relaxed:
                    if is_context_filter and len(skill_groups_relaxed) > 1:
                        relaxed_clauses.append(f"({' AND '.join(skill_groups_relaxed)})")
                    else:
                        relaxed_clauses.append(f"({' OR '.join(skill_groups_relaxed)})")

            # Execute relaxed query
            sql_relaxed = "SELECT resume_id FROM parsed_resumes"
            if relaxed_clauses:
                sql_relaxed += " WHERE " + " AND ".join(relaxed_clauses)
            else:
                # If no clauses are left (e.g., query was purely "Google employees"), fetch all or handle gracefully
                # We'll fetch all and let the LLM handle the "no specific names/skills" aspect
                pass

            print(f"   RELAXED SQL: {sql_relaxed}")
            print(f"   RELAXED Params: {relaxed_params}")

            cursor.execute(sql_relaxed, relaxed_params)
            results = cursor.fetchall()

            if results:
                print(f"   ✅ Recovered {len(results)} candidates after dropping filters: {', '.join(dropped_filters)}")
            else:
                print("   ❌ Still zero results after filter relaxation.")

    conn.close()

    candidate_ids = [row[0] for row in results]
    state["candidate_ids"] = candidate_ids
    state["dropped_filters"] = dropped_filters

    print(f"   ✅ Final: Found {len(candidate_ids)} candidates via SQL")

    # If no results and this was a sql_only query, mark for LLM SQL retry
    if not candidate_ids and state["search_strategy"] == "sql_only":
        state["use_llm_sql"] = True

    return state


def llm_sql_generation_node(state: AgentState) -> AgentState:
    """
    Node 2.5: Generate SQL query using LLM for complex queries

    Invoked when query analysis detects complexity (aggregations, comparisons, etc.)
    OR on retry when predefined SQL filters don't work.
    LLM gets complete schema and generates appropriate SQL.
    """

    # Only run if use_llm_sql flag is set
    if not state.get("use_llm_sql"):
        return state

    print("\n🤖 GENERATING SQL WITH LLM...")
    
    # Use comprehensive database schema
    schema_info = DATABASE_SCHEMA

    # Check if this is an aggregation query
    analysis = state.get("query_analysis", {})
    is_aggregation = analysis.get("is_aggregation_query", False)
    tool_action = state.get("tool_action", {})
    is_tool_action_with_search = bool(tool_action) and tool_action.get("needs_candidate_search", False)

    # Check if query has explicit names (for tool actions like email)
    entities = analysis.get("entities", {})
    has_explicit_names = bool(entities.get("names"))

    if is_aggregation:
        select_instruction = "2. For aggregation queries (how many, count, average), use COUNT(*), AVG(), SUM(), MAX(), MIN() - NOT resume_id"
    else:
        select_instruction = "2. Always SELECT resume_id for candidate search queries"

    # Add special instruction for tool actions with candidate search
    email_action_note = ""
    if is_tool_action_with_search and has_explicit_names:
        email_action_note = f"""
⚠️ CRITICAL: This is a TOOL ACTION query with explicit candidate names.
- Names mentioned: {', '.join(entities.get('names', []))}
- ONLY filter by candidate names using OR logic: (candidate_name LIKE '%Name1%' OR candidate_name LIKE '%Name2%')
- IGNORE any job titles, companies, or skills mentioned - those are for TOOL CONTENT, not candidate filtering
- Example: "Send email to John and Mary for ML Intern at Google" → Only filter by John OR Mary (ignore "ML Intern" and "Google")
"""
    
    sql_generation_prompt = f"""
You are a SQL expert. Generate a SQLite query to answer this user question.

DATABASE SCHEMA:
{schema_info}

USER QUESTION: {state["query"]}

{email_action_note}

RULES:
1. ONLY return the SQL query, nothing else
{select_instruction}
3. Use LIKE for text matching with % wildcards
4. For experience, use total_experience_years (it's a number)
5. For skills, search in the skills column (JSON array as text)
6. For phone numbers, use: REPLACE(REPLACE(REPLACE(phone, '-', ''), ' ', ''), '+', '') LIKE '%cleanednumber%'
7. For email, use: email LIKE '%emailpattern%'
8. For education/institute, search in education column: education LIKE '%institutename%'
9. For degree, search in education column: education LIKE '%degreename%'
10. For projects, search in projects column: projects LIKE '%projectname%'
11. For companies, search in work_experience column: work_experience LIKE '%companyname%'
12. For achievements/certifications, search in additional_information column

EXAMPLES:

CANDIDATE SEARCH QUERIES (return resume_id):
- "whose contact is 8374106843" → SELECT resume_id FROM parsed_resumes WHERE REPLACE(REPLACE(REPLACE(REPLACE(REPLACE(phone, '-', ''), ' ', ''), '(', ''), ')', ''), '+', '') LIKE '%8374106843%'
- "find email john@example.com" → SELECT resume_id FROM parsed_resumes WHERE email LIKE '%john@example.com%'
- "candidates with Python" → SELECT resume_id FROM parsed_resumes WHERE skills LIKE '%Python%'
- "who studied at IIT Delhi" → SELECT resume_id FROM parsed_resumes WHERE education LIKE '%IIT Delhi%'
- "MBA graduates" → SELECT resume_id FROM parsed_resumes WHERE education LIKE '%MBA%'
- "worked at Google" → SELECT resume_id FROM parsed_resumes WHERE work_experience LIKE '%Google%'
- "Python developers with 5+ years from IIT" → SELECT resume_id FROM parsed_resumes WHERE skills LIKE '%Python%' AND total_experience_years >= 5 AND education LIKE '%IIT%'

EMAIL ACTION QUERIES (filter by names ONLY - ignore job/company details):
- "Send email to Shubham Baghel and Anshika Chaudhary for ML Intern at Google" → SELECT resume_id FROM parsed_resumes WHERE (candidate_name LIKE '%Shubham%' AND candidate_name LIKE '%Baghel%') OR (candidate_name LIKE '%Anshika%' AND candidate_name LIKE '%Chaudhary%')
- "Email John Smith for Software Engineer position" → SELECT resume_id FROM parsed_resumes WHERE candidate_name LIKE '%John%' AND candidate_name LIKE '%Smith%'
- CRITICAL: Use proper parentheses for OR logic! Each full name in its own parentheses: (Name1_Part1 AND Name1_Part2) OR (Name2_Part1 AND Name2_Part2)

AGGREGATION QUERIES (return COUNT/AVG/SUM/MAX/MIN):
- "How many Python developers?" → SELECT COUNT(*) FROM parsed_resumes WHERE skills LIKE '%Python%'
- "How many candidates from IIT?" → SELECT COUNT(*) FROM parsed_resumes WHERE education LIKE '%IIT%'
- "What's the average experience?" → SELECT AVG(total_experience_years) FROM parsed_resumes
- "Average experience of Python developers?" → SELECT AVG(total_experience_years) FROM parsed_resumes WHERE skills LIKE '%Python%'
- "Total candidates with AWS" → SELECT COUNT(*) FROM parsed_resumes WHERE skills LIKE '%AWS%'

RANKING QUERIES (return resume_id with ORDER BY LIMIT):
- "Who has the most experience?" → SELECT resume_id FROM parsed_resumes ORDER BY total_experience_years DESC LIMIT 1
- "Top 5 candidates by experience" → SELECT resume_id FROM parsed_resumes ORDER BY total_experience_years DESC LIMIT 5

SQL QUERY:
"""

    try:
        response = llm.invoke(sql_generation_prompt)
        generated_sql = response.content.strip()

        # Clean up the SQL (remove markdown code blocks if present)
        if "```" in generated_sql:
            generated_sql = generated_sql.split("```")[1]
            if generated_sql.startswith("sql"):
                generated_sql = generated_sql[3:]
            generated_sql = generated_sql.strip()

        print(f"   Generated SQL: {generated_sql}")

        # Execute the generated SQL
        import os

        db_path = os.path.join(
            os.path.dirname(os.path.dirname(os.path.dirname(__file__))), "resumes.db"
        )
        conn = sqlite3.connect(db_path)
        cursor = conn.cursor()

        cursor.execute(generated_sql)
        results = cursor.fetchall()
        conn.close()

        # Check if this is an aggregation query
        analysis = state.get("query_analysis", {})
        is_aggregation = analysis.get("is_aggregation_query", False)
        
        if is_aggregation:
            # For aggregations, store the result value, not resume_ids
            aggregation_result = results[0][0] if results else 0
            state["aggregation_result"] = aggregation_result
            state["candidate_ids"] = []  # No candidate IDs for aggregations
            state["llm_generated_sql"] = generated_sql
            print(f"   ✅ LLM SQL aggregation result: {aggregation_result}")
        else:
            # For candidate searches, extract resume_ids
            candidate_ids = [row[0] for row in results]
            state["candidate_ids"] = candidate_ids
            state["llm_generated_sql"] = generated_sql
            print(f"   ✅ LLM SQL found {len(candidate_ids)} candidates")

    except Exception as e:
        print(f"   ❌ LLM SQL failed: {str(e)}")
        state["candidate_ids"] = []

    return state


def vector_search_node(state: AgentState) -> AgentState:
    """
    Node 3: Execute vector search with optional metadata filtering

    CRITICAL LOGIC:
    - sql_only: Skip vector search, use SQL results directly (SQL is authoritative)
    - vector_first: Use vector search with optional SQL filters (semantic ranking needed)
    - hybrid: Use vector search with SQL filters (combine exact + semantic)
    """
    
    # Skip vector search for aggregation queries (they just need the number, not candidate details)
    analysis = state.get("query_analysis", {})
    if analysis.get("is_aggregation_query", False):
        print("\n🔍 SKIPPING VECTOR SEARCH (aggregation query - no candidates needed)")
        state["search_results"] = {}
        return state

    # For sql_only strategy with SQL results, skip vector search entirely
    # Vector search would only reduce the results (top_k limitation)
    if state["search_strategy"] == "sql_only" and state["candidate_ids"]:
        print(f"\n🔍 SKIPPING VECTOR SEARCH ({state['search_strategy']} strategy)")
        print(f"   ✅ Using ALL {len(state['candidate_ids'])} candidates from SQL directly")
        print(f"   💡 Vector search would reduce results due to top_k limit")

        # Create mock search results with just the resume_ids from SQL
        state["search_results"] = {
            "ids": [[f"{rid}__direct" for rid in state["candidate_ids"]]],
            "metadatas": [[{"resume_id": rid} for rid in state["candidate_ids"]]],
            "documents": [["" for _ in state["candidate_ids"]]],
            "distances": [[0.0 for _ in state["candidate_ids"]]],
        }
        return state

    print("\n🔍 EXECUTING VECTOR SEARCH...")
    print(f"   Strategy: {state['search_strategy']} - Using semantic ranking")

    try:
        # Use vector store directly (not HybridResumeSearch) to apply ChromaDB filters correctly
        from vectorstore.chroma_store import ResumeVectorStore

        vector_store = ResumeVectorStore()

        # Prepare metadata filter if we have candidate IDs from SQL (hybrid strategy)
        vector_filters = None
        if state["candidate_ids"]:
            vector_filters = {"resume_id": {"$in": state["candidate_ids"]}}
            print(f"   🎯 Filtering to {len(state['candidate_ids'])} candidates from SQL")
            print(f"   ⚠️  Note: Will return top 10 most relevant (not all {len(state['candidate_ids'])})")

        # Execute search directly on vector store with proper filter format
        results = vector_store.search(
            query=state["vector_query"], top_k=10, filters=vector_filters
        )

        state["search_results"] = results

        print(f"   ✅ Found {len(results.get('ids', [[]])[0])} vector matches")
    except Exception as err:
        print(f"   ⚠️  Vector search unavailable: {err}")
        if state.get("candidate_ids"):
            print("   ↪️ Falling back to SQL-derived candidate set without vector ranking")
            state["search_results"] = {
                "ids": [[f"{rid}__direct" for rid in state["candidate_ids"]]],
                "metadatas": [[{"resume_id": rid} for rid in state["candidate_ids"]]],
                "documents": [["" for _ in state["candidate_ids"]]],
                "distances": [[0.0 for _ in state["candidate_ids"]]],
            }
        else:
            state["search_results"] = {}

    return state


def enrich_results_node(state: AgentState) -> AgentState:
    """
    Node 4: Fetch full resume data from SQLite and deduplicate
    
    Handles same resume appearing multiple times (different chunks) from vector search results
    """
    
    # Skip enrichment for aggregation queries
    analysis = state.get("query_analysis", {})
    if analysis.get("is_aggregation_query", False):
        print("\n💎 SKIPPING ENRICHMENT (aggregation query - returning aggregated value)")
        state["final_results"] = []
        state["should_retry"] = False
        return state

    print("\n💎 ENRICHING RESULTS...")

    if not state["search_results"] or not state["search_results"].get("metadatas"):
        state["final_results"] = []
        state["should_retry"] = True
        return state

    # Deduplicate by resume_id (keep ALL chunks for each resume)
    resume_chunks = {}  # {resume_id: [list of chunks]}
    metadatas = state["search_results"]["metadatas"][0]
    documents = state["search_results"]["documents"][0]
    distances = state["search_results"]["distances"][0]

    for meta, doc, dist in zip(metadatas, documents, distances):
        resume_id = meta.get("resume_id")
        
        if resume_id not in resume_chunks:
            resume_chunks[resume_id] = []
        
        # Store ALL chunks for this resume (not just highest scoring)
        resume_chunks[resume_id].append({
            "chunk_type": meta.get("chunk_type"),
            "chunk_text": doc,
            "distance": dist,
            "metadata": meta
        })

    unique_ids = list(resume_chunks.keys())
    print(
        f"   Deduplicated: {len(metadatas)} chunks → {len(unique_ids)} unique resumes"
    )

    # Fetch full data from SQLite
    import os

    db_path = os.path.join(
        os.path.dirname(os.path.dirname(os.path.dirname(__file__))), "resumes.db"
    )
    conn = sqlite3.connect(db_path)
    conn.row_factory = sqlite3.Row
    cursor = conn.cursor()

    placeholders = ",".join("?" * len(unique_ids))
    query = f"""
        SELECT 
            pr.*,
            d.raw_text
        FROM parsed_resumes pr
        LEFT JOIN documents d ON pr.document_id = d.document_id
        WHERE pr.resume_id IN ({placeholders})
    """

    cursor.execute(query, unique_ids)
    rows = cursor.fetchall()
    conn.close()

    final_results = []
    for row in rows:
        resume_dict = dict(row)
        resume_id = resume_dict['resume_id']
        
        # ✅ NEW: Add matched chunks to resume data
        resume_dict['matched_chunks'] = resume_chunks.get(resume_id, [])
        
        final_results.append(resume_dict)

    state["final_results"] = final_results

    print(f"   ✅ Enriched {len(final_results)} resumes with full data + matched chunks")

    # Decide if we should retry
    state["should_retry"] = len(final_results) == 0

    return state


def fetch_context_candidates_node(state: AgentState) -> AgentState:
    """
    Node: Fetch candidate data from conversation context for Q&A queries
    
    This node only runs for Q&A queries that skip search.
    It fetches the full resume data for candidates from previous conversation.
    """
    
    print("\n💬 FETCHING CANDIDATES FROM CONVERSATION CONTEXT...")
    
    # Get candidate IDs from conversation context (set by analyze_query_node)
    context = state.get("conversation_context", {})
    candidate_ids = context.get("candidate_ids", [])
    candidate_names = context.get("candidate_names", [])
    
    if not candidate_ids:
        print("   ⚠️  No candidates in conversation context")
        state["final_results"] = []
        return state
    
    print(f"   📋 Found {len(candidate_ids)} candidates from previous conversation")
    for i, (cid, name) in enumerate(zip(candidate_ids, candidate_names), 1):
        print(f"      {i}. {name} ({cid})")
    
    # Fetch full data from SQLite
    import os
    
    db_path = os.path.join(
        os.path.dirname(os.path.dirname(os.path.dirname(__file__))), "resumes.db"
    )
    conn = sqlite3.connect(db_path)
    conn.row_factory = sqlite3.Row
    cursor = conn.cursor()

    placeholders = ",".join("?" * len(candidate_ids))
    query = f"""
        SELECT 
            pr.*,
            d.raw_text
        FROM parsed_resumes pr
        LEFT JOIN documents d ON pr.document_id = d.document_id
        WHERE pr.resume_id IN ({placeholders})
    """

    cursor.execute(query, candidate_ids)
    rows = cursor.fetchall()
    conn.close()

    final_results = []
    for row in rows:
        resume_dict = dict(row)
        # No matched_chunks for Q&A queries (we're not searching)
        resume_dict['matched_chunks'] = []
        final_results.append(resume_dict)

    state["final_results"] = final_results
    print(f"   ✅ Fetched full data for {len(final_results)} candidates")
    
    return state


# ============= Helper functions for execute_mcp_tool_node =============

def _extract_tool_fields(query: str, field_names: list, candidate_names: list = None) -> dict:
    """
    Generic LLM-based field extraction. Works for any server's required_fields.
    Returns {field_name: value_or_None} for each field in field_names.
    Falls back to simple "Key: Value" regex parsing if LLM fails.
    candidate_names: names to EXCLUDE from extraction (they are candidates, not field values).
    """
    if not field_names:
        return {}

    # --- Attempt 1: simple regex for "Key: Value" patterns (fast, no LLM) ---
    import re
    regex_result = {}
    for f in field_names:
        # Match "Job Role: xyz" or "job_role: xyz" patterns
        label_variants = [
            f.replace("_", " "),                      # job_role -> job role
            f.replace("_", " ").title(),               # job_role -> Job Role
            f,                                         # job_role
        ]
        for label in label_variants:
            pattern = rf"(?i){re.escape(label)}\s*:\s*(.+?)(?:,\s*(?:\w[\w ]*:)|$)"
            m = re.search(pattern, query)
            if m:
                regex_result[f] = m.group(1).strip()
                break

    # If regex found all fields, skip LLM entirely
    if all(regex_result.get(f) for f in field_names):
        print("   ✅ All fields extracted via regex (fast path)")
        return regex_result

    # --- Attempt 2: LLM extraction ---
    fields_list = "\n".join(f"- {f}" for f in field_names)

    # Build candidate-awareness context so LLM doesn't confuse candidate names with field values
    candidate_note = ""
    if candidate_names:
        names_str = ", ".join(candidate_names)
        candidate_note = (f"\n\nIMPORTANT: The following are CANDIDATE names (the person receiving the action), "
                         f"NOT field values. Do NOT assign them to any field: {names_str}")

    extraction_prompt = ChatPromptTemplate.from_messages([
        ("system", "Extract the following fields from the user query.\n"
                   "Return ONLY values that are EXPLICITLY mentioned as field values. "
                   "Return null for anything not clearly provided.\n\n"
                   f"Fields to extract:\n{fields_list}"
                   f"{candidate_note}\n\n"
                   "Return valid JSON with those exact keys."),
        ("user", "Query: {query}")
    ])

    chain = extraction_prompt | llm | JsonOutputParser()
    try:
        result = chain.invoke({"query": query})
        llm_result = {f: result.get(f) for f in field_names}
    except Exception as e:
        print(f"   ⚠️  Field extraction failed: {e}")
        llm_result = {f: None for f in field_names}

    # Merge: prefer regex (exact) over LLM, fill gaps from LLM
    merged = {}
    for f in field_names:
        merged[f] = regex_result.get(f) or llm_result.get(f)
    return merged


def _build_ask_message(candidate_names: list, missing_fields: list, field_examples: dict) -> str:
    """Build the message listing ALL missing fields for the user to fill in."""
    if candidate_names:
        msg = f"📧 **Preparing to send invitation to {', '.join(candidate_names)}**\n\n"
    else:
        msg = "📋 **Please provide the following details:**\n\n"
    msg += "Please provide the following details:\n\n"
    for i, fk in enumerate(missing_fields, 1):
        meta = field_examples.get(fk, {})
        label = meta.get("label", fk.replace("_", " ").title())
        example = meta.get("example", "")
        msg += f"{i}. **{label}:** ({example})\n"
    return msg


def _format_success_message(sent: list, failed: list, fields: dict, server_config: dict) -> str:
    """Format a user-facing success message after MCP tool execution."""
    server_name = server_config.get("name", "Tool")
    parts = [f"✅ **{server_name} - {len(sent)} successful!**\n"]
    non_null = {k: v for k, v in fields.items() if v}
    if non_null:
        parts.append("📋 **Details:**")
        for k, v in non_null.items():
            parts.append(f"- {k.replace('_', ' ').title()}: {v}")
        parts.append("")
    parts.append("**Results:**")
    for r in sent:
        icon = "✅" if r.get("status") == "sent" else "📄"
        parts.append(f"  {icon} {r.get('candidate', '')} ({r.get('to', '')}) - {r.get('message', '')}")
    if failed:
        parts.append("\n**Failed:**")
        for r in failed:
            parts.append(f"  ❌ {r.get('candidate', '')} - {r.get('message', 'Unknown error')}")
    return "\n".join(parts)


def _extract_name_from_query(query: str, email: str) -> str:
    """
    Try to extract a candidate name from the query when a direct email is provided.
    Falls back to the email prefix (without leading digits).
    """
    import re
    query_without_email = query.replace(email, "").strip()
    patterns = [
        r"(?:send|invite|to)\s+([A-Z][a-z]+(?:\s+[A-Z][a-z]+)?)\s+(?:at|@|on)",
        r"invitation\s+to\s+([A-Z][a-z]+(?:\s+[A-Z][a-z]+)?)",
        r"to\s+([A-Z][a-z]+(?:\s+[A-Z][a-z]+)?)\s*(?:,|at|@)",
    ]
    for pattern in patterns:
        match = re.search(pattern, query_without_email, re.IGNORECASE)
        if match:
            return match.group(1).strip()
    # Fallback: strip leading digits from email prefix
    prefix = re.sub(r'^\d+', '', email.split('@')[0])
    return prefix.capitalize()


# ─────────────────────────────────────────────────────────────
# Generic MCP Tool Node  (replaces send_email_via_mcp_node)
# ─────────────────────────────────────────────────────────────

def execute_mcp_tool_node(state: AgentState) -> AgentState:
    """
    Universal MCP Tool Executor Node.

    Schema-driven: discovers tool name, required fields, and field metadata
    from the live server via tools/list — like Claude Desktop does.
    Config (mcp_config.json) only provides: script path, trigger keywords,
    needs_candidate_search, auto_fields.

    To add a new MCP server:
      1. Add entry to MCP/mcp_config.json  (script + keywords)
      2. Create the FastMCP server script  (tool signatures = the schema)
      Zero changes needed in this file.
    """  # noqa: D205
    from mcp_infra.registry import MCPRegistry
    from mcp_infra.executor import MCPExecutor

    print("\n🔧 EXECUTING MCP TOOL...")

    tool_action = state.get("tool_action", {})
    server_id = tool_action.get("server_id")
    if not server_id:
        state["answer"] = "❌ No tool action server detected."
        return state

    registry = MCPRegistry()
    executor = MCPExecutor()

    server_config = registry.get_server_config(server_id)
    script_path = registry.get_script_path(server_id)
    needs_candidate_search = server_config.get("needs_candidate_search", False)

    # ─── LIVE SCHEMA DISCOVERY ─────────────────────────────────
    # Discover tool name, required fields, and field examples from the server itself
    tool_name = registry.get_tool_name(server_id)
    required_fields = registry.get_required_fields(server_id)
    field_examples = registry.get_field_examples(server_id)

    print(f"   🎯 Server: {server_config.get('name', server_id)}")
    print(f"   🔧 Tool  : {tool_name}")
    print(f"   📋 Fields : {required_fields}")

    # ─── STEP 1: Load pending action ───────────────────────────
    conversation_context = state.get("conversation_context", {})
    pending_action = conversation_context.get("pending_tool_action")

    # ─── STEP 2: Extract fields from current query (LLM) ───────
    # Pass candidate names so LLM doesn't confuse them with field values (e.g. interviewer_name)
    known_candidates = [r.get("candidate_name", "") for r in state.get("final_results", [])]
    if pending_action:
        known_candidates += [c.get("candidate_name", "") for c in pending_action.get("candidates", [])]
    known_candidates = [n for n in known_candidates if n]  # remove blanks
    extracted = _extract_tool_fields(state["query"], required_fields, candidate_names=known_candidates)

    # ─── STEP 3: Merge current + pending fields ─────────────────
    if pending_action and pending_action.get("server_id") == server_id:
        print("   🔄 Merging with pending tool action fields...")
        collected = pending_action.get("collected_fields", {})
        merged_fields = {
            f: extracted.get(f) or collected.get(f)
            for f in required_fields
        }
        candidates = pending_action.get("candidates", state.get("final_results", []))
    else:
        merged_fields = {f: extracted.get(f) for f in required_fields}
        candidates = state.get("final_results", []) if needs_candidate_search else []



    # ─── STEP 4: Resolve candidates ────────────────────────────
    if needs_candidate_search:
        # Check if user directly provided an email in the current query.
        # If so, always override candidates to ONLY that email — even if
        # final_results has data from a prior search (avoids bulk-sending).
        import re as _re
        _direct_email_match = _re.search(
            r'\b[A-Za-z0-9._%+\-]+@[A-Za-z0-9.\-]+\.[A-Za-z]{2,}\b',
            state.get("query", "")
        )
        if _direct_email_match:
            _direct_email = _direct_email_match.group(0)
            print(f"   📧 Direct email detected in query: {_direct_email} — overriding candidate list")
            candidates = [{
                "resume_id": "external_" + _direct_email.replace("@", "_at_"),
                "candidate_name": _extract_name_from_query(state.get("query", ""), _direct_email),
                "email": _direct_email,
            }]
        elif not candidates:
            qa = state.get("query_analysis", {})
            ents = qa.get("entities", {})
            ext_email = ents.get("email")
            ext_name = (ents.get("names") or [None])[0]
            if ext_email:
                candidates = [{
                    "resume_id": "external_" + ext_email.replace("@", "_at_"),
                    "candidate_name": ext_name or "Candidate",
                    "email": ext_email,
                }]
            else:
                state["answer"] = "❌ No candidate found. Please specify a candidate name or email."
                return state

        valid = [c for c in candidates if c.get("email") and c.get("email") != "Not available"]
        skipped = [c.get("candidate_name") for c in candidates if c not in valid]
        if skipped:
            print(f"   ⚠️  Skipping (no email): {skipped}")
        if not valid:
            state["answer"] = "❌ None of the found candidates have email addresses on file."
            return state
        candidates = valid
        candidate_names = [c.get("candidate_name", "Unknown") for c in candidates]
    else:
        candidate_names = []

    # ─── STEP 5+6: Call server, handle response ─────────────────
    def _save_pending_and_ask(missing_fields_list, candidate_names_list):
        """Save pending action and build ask-message."""
        if "conversation_context" not in state:
            state["conversation_context"] = {}
        state["conversation_context"]["pending_tool_action"] = {
            "server_id": server_id,
            "collected_fields": merged_fields,
            "candidates": candidates,
            "missing_fields": missing_fields_list,
        }
        state["answer"] = _build_ask_message(candidate_names_list, missing_fields_list, field_examples)
        print(f"   💬 Missing fields: {missing_fields_list}")

    if needs_candidate_search:
        results = []
        for candidate in candidates:
            params = {"resume_id": candidate.get("resume_id"), **merged_fields}
            print(f"   📤 {tool_name} → {candidate.get('candidate_name')}...")
            resp = executor.execute(script_path, tool_name, params)
            resp["candidate"] = candidate.get("candidate_name", "Unknown")
            results.append(resp)

        missing_resp = next((r for r in results if r.get("status") == "missing_fields"), None)
        if missing_resp:
            _save_pending_and_ask(missing_resp.get("missing_fields", []), candidate_names)
            return state

        sent   = [r for r in results if r.get("status") in ("sent", "draft_only", "success")]
        failed = [r for r in results if r.get("status") == "error"]
        if sent:
            state["tool_executed"] = True
            state.get("conversation_context", {}).pop("pending_tool_action", None)
            state["answer"] = _format_success_message(sent, failed, merged_fields, server_config)
        else:
            err = failed[0].get("message", "Unknown error") if failed else "No results"
            state["answer"] = f"❌ {server_config.get('name', server_id)} failed: {err}"
    else:
        resp = executor.execute(script_path, tool_name, merged_fields)
        if resp.get("status") == "missing_fields":
            _save_pending_and_ask(resp.get("missing_fields", []), [])
        elif resp.get("status") in ("success", "sent", "draft_only"):
            state["tool_executed"] = True
            # Store tool response for follow-up questions
            if "conversation_context" not in state:
                state["conversation_context"] = {}
            state["conversation_context"].pop("pending_tool_action", None)
            state["conversation_context"]["last_tool_response"] = {
                "server_id": server_id,
                "server_name": server_config.get("name", server_id),
                "response": resp,
                "query": state["query"],
            }
            # Build rich answer from all response data (not just "message")
            answer_parts = [f"✅ {resp.get('message', 'Done!')}"]
            # Include any extra structured data the server returned
            skip_keys = {"status", "message"}
            for key, value in resp.items():
                if key in skip_keys:
                    continue
                label = key.replace("_", " ").title()
                if isinstance(value, dict):
                    # Render dict as bullet list (e.g., profile info)
                    answer_parts.append(f"\n**{label}:**")
                    for k, v in value.items():
                        answer_parts.append(f"- {k.replace('_', ' ').title()}: {v}")
                elif isinstance(value, list):
                    # Render list (e.g., repos, languages)
                    answer_parts.append(f"\n**{label}:**")
                    for item in value[:10]:
                        if isinstance(item, dict):
                            summary = ", ".join(f"{k}: {v}" for k, v in item.items())
                            answer_parts.append(f"- {summary}")
                        else:
                            answer_parts.append(f"- {item}")
                elif isinstance(value, str) and len(value) > 100:
                    # Long text (e.g., job_description) — render as block
                    answer_parts.append(f"\n{value}")
                else:
                    answer_parts.append(f"- **{label}:** {value}")
            state["answer"] = "\n".join(answer_parts)
        else:
            state["answer"] = f"❌ {resp.get('message', 'Unknown error')}"

    return state


def generate_answer_node(state: AgentState) -> AgentState:
    """
    Node 5: Generate natural language answer using LLM

    Uses existing answer generation function
    """

    print("\n🤖 GENERATING ANSWER...")
    
    # Check if this is a follow-up question about the last tool response
    conversation_context = state.get("conversation_context", {})
    last_tool = conversation_context.get("last_tool_response")
    if last_tool and not state.get("final_results"):
        # No search was run — answer from stored tool data
        print(f"   💬 Follow-up about {last_tool['server_name']}")
        # Use LLM to answer based on the stored response
        from langchain_openai import ChatOpenAI
        from langchain_core.messages import SystemMessage, HumanMessage
        llm = ChatOpenAI(model="gpt-4o-mini", temperature=0)
        import json
        resp_json = json.dumps(last_tool["response"], indent=2)
        answer = llm.invoke([
            SystemMessage(content="You are answering a follow-up question about a tool response. "
                                "Answer based on the data provided. Be concise and direct."),
            HumanMessage(content=f"Previous tool: {last_tool['server_name']}\n"
                               f"Previous query: {last_tool['query']}\n"
                               f"Tool response data:\n{resp_json}\n\n"
                               f"Follow-up question: {state['query']}\n\n"
                               f"Answer the question using ONLY the data above.")
        ]).content
        state["answer"] = answer
        print(f"   ✅ Follow-up answer generated")
        return state
    
    # Handle aggregation queries - return the number directly
    analysis = state.get("query_analysis", {})
    if analysis.get("query_type") == "greeting":
        state["answer"] = "Hello! I'm here to help you with your resume-related queries."
        return state

    if analysis.get("is_jd_match_query", False) or analysis.get("query_type") == "jd_match":
        jd_info = state.get("selected_jd", {})
        ranked = state.get("jd_match_results", [])

        if not jd_info:
            state["answer"] = "I couldn't resolve a job description for matching."
            return state

        if not ranked:
            state["answer"] = "No resume matches were found for the selected JD."
            return state

        lines = [
            f"Top {len(ranked)} resume matches for **{jd_info.get('job_title', 'Selected JD')}** (JD: {jd_info.get('jd_id', 'unknown')})",
            "",
        ]

        for idx, candidate in enumerate(ranked, 1):
            name = candidate.get("candidate_name") or "Unknown candidate"
            score = candidate.get("match_percentage", 0)
            fit = candidate.get("fit_label", "Fit not classified")
            exp = candidate.get("total_experience_years")
            reasons = candidate.get("match_reasons") or []
            reason_text = "; ".join(reasons[:2]) if reasons else "No detailed reason generated"

            lines.append(
                f"{idx}. {name} | Score: {score}% | {fit} | Experience: {exp if exp is not None else 'N/A'} years"
            )
            lines.append(f"   Reason: {reason_text}")

        state["answer"] = "\n".join(lines)
        print(f"   ✅ JD match answer generated with {len(ranked)} ranked candidates")
        return state

    # Handle JD Info queries - show JD details
    if analysis.get("is_jd_info_query", False) or analysis.get("query_type") == "jd_info":
        jd_info = state.get("jd_info", {})
        if not jd_info:
            jd_info = state.get("conversation_context", {}).get("active_jd", {})

        if not jd_info:
            state["answer"] = "I couldn't find any job description information."
            return state

        subintent = _classify_jd_info_subintent(state.get("query", ""))

        required = _normalize_text_list(jd_info.get("required_skills", []))
        nice_to_have = _normalize_text_list(jd_info.get("nice_to_have_skills", []))
        responsibilities = _normalize_text_list(jd_info.get("responsibilities", []))
        role_summary = str(jd_info.get("role_summary", "")).strip()

        if subintent == "required_skills":
            if required:
                lines = [
                    f"Must-have skills for **{jd_info.get('job_title', 'this JD')}**:",
                    *[f"- {skill}" for skill in required],
                ]
                state["answer"] = "\n".join(lines)
            else:
                state["answer"] = "I couldn't find explicit must-have skills in this JD."
            print("   ✅ JD required-skills answer generated")
            return state

        if subintent == "preferred_skills":
            if nice_to_have:
                lines = [
                    f"Preferred (nice-to-have) skills for **{jd_info.get('job_title', 'this JD')}**:",
                    *[f"- {skill}" for skill in nice_to_have],
                ]
                state["answer"] = "\n".join(lines)
            else:
                state["answer"] = "This JD does not list any preferred/nice-to-have skills."
            print("   ✅ JD preferred-skills answer generated")
            return state

        if subintent == "role":
            lines = [
                f"Role: **{jd_info.get('job_title', 'Unknown Role')}**",
            ]
            if role_summary:
                lines.extend(["", role_summary])
            if responsibilities:
                lines.extend(["", "Key responsibilities:"])
                lines.extend([f"- {item}" for item in responsibilities[:6]])
            state["answer"] = "\n".join(lines)
            print("   ✅ JD role answer generated")
            return state

        if subintent == "summary" or subintent == "full":
            from langchain_core.messages import SystemMessage, HumanMessage
            import json
            
            jd_json = json.dumps(jd_info, indent=2)
            
            summary_prompt = [
                SystemMessage(content=(
                    "You are a helpful HR assistant. "
                    "Provide a helpful answer about the job description based strictly on the provided JD data. "
                    "Follow the user's formatting constraints (e.g., 'in 5 bullet points'). "
                    "Do NOT invent details not found in the JD."
                )),
                HumanMessage(content=(
                    f"User Query: {state['query']}\n\n"
                    f"JD Data:\n{jd_json}"
                ))
            ]
            
            try:
                response = answer_llm.invoke(summary_prompt)
                state["answer"] = response.content
                print("   ✅ JD LLM-generated answer created")
                return state
            except Exception as e:
                print(f"   ⚠️  LLM JD summary failed: {e}. Falling back to default format.")
                
            # Fallback to default summary if LLM fails
            lines = [
                f"JD Summary: **{jd_info.get('job_title', 'Unknown Role')}**",
            ]
            if jd_info.get("location"):
                lines.append(f"Location: {jd_info['location']}")
            if role_summary:
                lines.extend(["", role_summary])
            if required:
                lines.extend(["", "Must-have skills:"])
                lines.extend([f"- {skill}" for skill in required])
            if nice_to_have:
                lines.extend(["", "Preferred skills:"])
                lines.extend([f"- {skill}" for skill in nice_to_have])
            if responsibilities:
                lines.extend(["", "Top responsibilities:"])
                lines.extend([f"- {item}" for item in responsibilities[:3]])
            state["answer"] = "\n".join(lines)
            print("   ✅ JD summary answer generated (fallback)")
            return state

        if subintent == "one_line_summary":
            core_skills = ", ".join(required[:3]) if required else "core backend skills"
            location = jd_info.get("location")
            location_text = f" in {location}" if location else ""
            state["answer"] = (
                f"{jd_info.get('job_title', 'This role')} is a backend API role{location_text} focused on {core_skills}."
            )
            print("   ✅ JD one-line summary answer generated")
            return state

        if subintent == "confirmation":
            role_name = jd_info.get("job_title", "this role")
            must_have_preview = ", ".join(required[:3]) if required else "core backend skills"
            state["answer"] = (
                f"Yes. It is primarily a **{role_name}** requirement focused on {must_have_preview}."
            )
            print("   ✅ JD confirmation answer generated")
            return state

        if subintent == "location":
            location = jd_info.get("location", "")
            role_name = jd_info.get("job_title", "this role")
            if location:
                state["answer"] = f"The **{role_name}** position is located in **{location}**."
            else:
                state["answer"] = f"No specific location is listed for **{role_name}**."
            print("   ✅ JD location answer generated")
            return state

        if subintent == "skill_check":
            # Extract the skill being asked about from the query
            user_query = state.get("query", "").lower()
            # Check against both required and nice-to-have lists
            asked_skill = None
            for skill in required + nice_to_have:
                if skill.lower() in user_query:
                    asked_skill = skill
                    break

            if asked_skill:
                is_required = any(asked_skill.lower() == s.lower() for s in required)
                is_nice = any(asked_skill.lower() == s.lower() for s in nice_to_have)
                if is_required:
                    state["answer"] = f"Yes, **{asked_skill}** is a **must-have** (required) skill for this role."
                elif is_nice:
                    state["answer"] = f"No, **{asked_skill}** is listed as a **nice-to-have** (preferred) skill, not a must-have."
                else:
                    state["answer"] = f"**{asked_skill}** is not explicitly listed in the JD requirements."
            else:
                state["answer"] = (
                    f"Must-have skills: {', '.join(required[:5]) if required else 'none listed'}.\n"
                    f"Nice-to-have skills: {', '.join(nice_to_have[:5]) if nice_to_have else 'none listed'}."
                )
            print("   ✅ JD skill-check answer generated")
            return state

        # Build formatted JD info response
        lines = [
            f"## 📋 Job Description: {jd_info.get('job_title', 'Unknown Role')}",
            f"**JD ID:** {jd_info.get('jd_id', 'unknown')}",
            f"**Status:** {jd_info.get('status', 'active')}",
            "",
        ]

        if jd_info.get("job_level"):
            lines.append(f"**Level:** {jd_info['job_level']}")
        if jd_info.get("location"):
            lines.append(f"**Location:** {jd_info['location']}")

        lines.append("")

        # Required Skills
        required = _normalize_text_list(jd_info.get("required_skills", []))
        if required:
            lines.append("### Required Skills")
            for skill in required:
                lines.append(f"- {skill}")
            lines.append("")

        # Nice-to-have Skills
        nice_to_have = _normalize_text_list(jd_info.get("nice_to_have_skills", []))
        if nice_to_have:
            lines.append("### Nice-to-Have Skills")
            for skill in nice_to_have:
                lines.append(f"- {skill}")
            lines.append("")

        # Role Summary
        if jd_info.get("role_summary"):
            lines.append("### Role Summary")
            lines.append(jd_info["role_summary"])
            lines.append("")

        # Responsibilities
        if responsibilities:
            lines.append("### Responsibilities")
            for resp_item in responsibilities:
                lines.append(f"- {resp_item}")
            lines.append("")

        # Company Overview
        if jd_info.get("company_overview"):
            lines.append("### Company Overview")
            lines.append(jd_info["company_overview"])
            lines.append("")

        state["answer"] = "\n".join(lines)
        print(f"   ✅ JD info answer generated for {jd_info.get('job_title', 'Unknown')}")
        return state

    if analysis.get("is_aggregation_query", False):
        aggregation_result = state.get("aggregation_result", 0)
        query_lower = state["query"].lower()
        
        # Format answer based on query type
        if "how many" in query_lower or "count" in query_lower:
            state["answer"] = f"There are **{aggregation_result} candidates** matching your criteria."
        elif "average" in query_lower or "avg" in query_lower:
            state["answer"] = f"The average is **{aggregation_result:.2f}**."
        elif "total" in query_lower or "sum" in query_lower:
            state["answer"] = f"The total is **{aggregation_result}**."
        else:
            state["answer"] = f"Result: **{aggregation_result}**"
        
        print(f"   ✅ Aggregation answer: {aggregation_result}")
        return state

    if not state["final_results"]:
        state["answer"] = (
            "I couldn't find any candidates matching your criteria. Try:\n- Broadening your search terms\n- Removing some filters\n- Checking for typos in names or skills"
        )
        return state

    # ✅ SMART FORMAT SELECTION: Detailed profiles vs compact list
    total_candidates = len(state["final_results"])
    query_lower = state["query"].lower()
    
    # Check if user wants LIST ALL candidates (not detailed profiles)
    wants_list_all = any(phrase in query_lower for phrase in [
        "list all", "show all", "all candidates", "find all", 
        "list candidates", "all of them"
    ])
    
    # Check if user wants DETAILED profiles
    wants_detailed = any(phrase in query_lower for phrase in [
        "detailed profile", "tell me about", "show me details", "full details",
        "in detail", "detailed information", "tell me more about",
        "who is", "what is their", "what are their", "show their",
        "education", "experience", "projects", "skills of", "background",
        "contact", "email", "phone", "work history", "achievements", "certifications"
    ])
    
    # Check if user wants only TOP N candidates
    wants_top_n = any(phrase in query_lower for phrase in [
        "top 5", "best 5", "top five", "best five", "show me 5",
        "top 3", "best 3", "top three"
    ])
    
    # DECISION LOGIC:
    # 1. If explicitly wants "list all" → compact list format, ALL candidates
    if wants_list_all:
        print(f"   📋 'List all' detected: Showing ALL {total_candidates} candidates in compact format")
        results_to_use = state["final_results"]
        format_as_list = True
    
    # 2. If wants detailed info (education/experience/etc) → detailed profiles
    elif wants_detailed:
        # ✅ FIXED: If user wants ALL previous ("their", "these"), show ALL, don't limit to 5
        wants_all_from_previous = any(phrase in query_lower for phrase in [
            "their", "these", "those", "all of them", "all their", "all these"
        ])
        print(f"   📄 Detailed query: Showing all {total_candidates} candidates")
        results_to_use = state["final_results"]
        format_as_list = False
    
    # 3. If wants top N → detailed profiles, limited
    elif wants_top_n:
        results_to_use = state["final_results"][:5]
        format_as_list = False
        print(f"   📄 Top N query: Showing top 5 of {total_candidates} candidates")
    
    # 4. DEFAULT: If 10+ candidates → compact list, otherwise detailed profiles
    elif total_candidates >= 10:
        print(f"   📋 Many results ({total_candidates}): Using compact format")
        results_to_use = state["final_results"]
        format_as_list = True
    
    else:
        # 1-9 candidates: Show detailed profiles
        print(f"   📄 Few results ({total_candidates}): Using detailed format")
        results_to_use = state["final_results"]
        format_as_list = False
    
    query_lower = state["query"].lower()
    
    # Check if user explicitly wants ALL previous candidates
    # Include "their" which is commonly used ("show me their education")
    wants_all_previous = any(phrase in query_lower for phrase in [
        "all of these", "all of them", "all these", "all candidates",
        "for all", "of all", "each of these", "every one of them",
        "all their", "all previous", "for each", "their education",
        "their experience", "their skills", "their projects", "their work",
        "their contact", "show me their", "what are their"
    ])
    
    is_specific_query = any(keyword in query_lower for keyword in [
        "this candidate", "the candidate", "that candidate"
    ])
    
    # ✅ INTENT-AWARE FILTERING: Only filter to single candidate if this is truly a follow-up query
    # NOT a search query asking "which candidates have X"
    # AND NOT when user wants ALL previous candidates
    
    # Indicators of SEARCH queries (should show ALL results):
    search_indicators = [
        "which candidate",
        "find candidate",
        "show me candidate",
        "list candidate",
        "who has",
        "candidates with",
        "find me",
        "search for"
    ]
    
    is_search_query = any(indicator in query_lower for indicator in search_indicators)
    
    # Skip single-candidate filtering if user wants ALL previous
    if wants_all_previous:
        print(f"   📋 'ALL' detected: Showing information for ALL {len(results_to_use)} candidates")
    # If asking about specific candidate AND we have candidate_ids filter (follow-up from chat history)
    elif is_specific_query and state["sql_filters"].get("candidate_ids") and not is_search_query:
        candidate_id = state["sql_filters"]["candidate_ids"][0]
        # Filter to only this candidate
        results_to_use = [r for r in results_to_use if r["resume_id"] == candidate_id]
        print(f"   🎯 Specific query detected: Showing ONLY candidate {candidate_id}")
    
    # OR if name was specified AND it's a follow-up query (not a search)
    # Example: "show me John's education" → filter to John only
    # But NOT: "which candidate named John has Python" → show John in results but don't filter others
    elif state["query_analysis"].get("entities", {}).get("names") and not is_search_query:
        specified_names = state["query_analysis"]["entities"]["names"]
        
        # ✅ FIXED: If multiple names mentioned, show ALL of them (don't filter to one)
        if len(specified_names) > 1:
            # Multiple names explicitly mentioned - show all matching candidates
            print(f"   📋 Multiple names query: Showing ALL {len(results_to_use)} candidates")
        else:
            # Single name - check if we should filter to just that candidate
            specified_name = specified_names[0].lower()
            name_matches = [r for r in results_to_use if specified_name in r.get("candidate_name", "").lower()]
            if len(name_matches) == 1:
                results_to_use = name_matches
                print(f"   🎯 Single candidate query: Showing ONLY {name_matches[0]['candidate_name']}")
            elif len(name_matches) > 1:
                # Multiple candidates with similar names - show all
                print(f"   📋 Found {len(name_matches)} candidates matching '{specified_name}' - showing all")
    
    answer = generate_answer(
        query=state["query"], 
        search_results=results_to_use, 
        conversation_history=state.get("chat_history", []),
        format_as_list=format_as_list, 
        dropped_filters=state.get("dropped_filters", [])
    )

    state["answer"] = answer

    print(f"   ✅ Answer generated ({len(answer)} chars)")

    return state


def should_retry_node(state: AgentState) -> Literal["retry", "end"]:
    """
    Router: Decide whether to retry with different strategy

    On retry, enable LLM SQL generation for complex queries
    """

    if state["should_retry"] and state.get("retry_count", 0) < 1:
        print("\n🔄 NO RESULTS - RETRYING WITH LLM SQL GENERATION...")
        state["retry_count"] = state.get("retry_count", 0) + 1
        state["use_llm_sql"] = True  # Enable LLM SQL on retry

        # Switch strategy to vector_first if SQL failed
        if state["search_strategy"] == "sql_only":
            state["search_strategy"] = "vector_first"
        else:
            state["search_strategy"] = "hybrid"

        return "retry"

    return "end"


# ============= Build Graph =============


def create_intelligent_agent() -> StateGraph:
    """
    Build the LangGraph workflow for intelligent resume search

    Flow:
    1. Analyze query → 2. SQL filter → 2.5. LLM SQL (if needed) → 3. Vector search →
    4. Enrich results → 5. Generate answer → Retry if needed
    """

    workflow = StateGraph(AgentState)

    # Add nodes
    workflow.add_node("analyze_query", analyze_query_node)
    workflow.add_node("validate_assumptions", validate_assumptions_node)  # NEW
    workflow.add_node("generate_clarification", generate_clarification_node)  # NEW
    workflow.add_node("jd_resume_match", jd_resume_match_node)
    workflow.add_node("jd_info", jd_info_node)
    workflow.add_node("sql_filter", sql_filter_node)
    workflow.add_node("llm_sql_generation", llm_sql_generation_node)  # NEW
    workflow.add_node("vector_search", vector_search_node)
    workflow.add_node("enrich_results", enrich_results_node)
    workflow.add_node("fetch_context_candidates", fetch_context_candidates_node)  # NEW: For Q&A queries
    workflow.add_node("execute_mcp_tool", execute_mcp_tool_node)  # Generic: handles any MCP server
    workflow.add_node("generate_answer", generate_answer_node)

    # Define edges
    # analyzer -> validate_assumptions first
    workflow.set_entry_point("analyze_query")
    workflow.add_edge("analyze_query", "validate_assumptions")

    # Routing out of validate_assumptions
    def route_after_validation(state: AgentState) -> Literal["generate_clarification", "route_to_next"]:
        """Route to clarification if ambiguous, else proceed"""
        analysis = state.get("query_analysis", {})
        if analysis.get("is_ambiguous", False) or analysis.get("clarification_needed"):
            return "generate_clarification"
        return "route_to_next"
        
    workflow.add_conditional_edges(
        "validate_assumptions",
        route_after_validation,
        {
            "generate_clarification": "generate_clarification",
            "route_to_next": "route_to_next_jump" # Virtual node to run the rest of logic
        }
    )

    # Need a dummy node to jump off
    def dummy_route_node(state: AgentState) -> AgentState:
        return state
    
    workflow.add_node("route_to_next_jump", dummy_route_node)
    
    # After clarification, end or generate answer
    workflow.add_edge("generate_clarification", END)

    def route_after_analysis(state: AgentState) -> Literal["jd_resume_match", "jd_info", "sql_filter", "fetch_context_candidates", "execute_mcp_tool", "generate_answer"]:
        """Route based on query type"""
        import re as _re
        conversation_context = state.get("conversation_context", {})
        last_tool = conversation_context.get("last_tool_response")
        tool_action = state.get("tool_action", {})

        # ── Follow-up about a previous tool result (no new tool triggered) ──
        if last_tool and not tool_action:
            print("   💬 Detected follow-up question about previous tool response")
            return "generate_answer"

        analysis = state.get("query_analysis", {})

        # JD matching query (rank resumes against JD)
        if analysis.get("is_jd_match_query", False):
            return "jd_resume_match"

        # JD info query (asking about the JD itself)
        if analysis.get("is_jd_info_query", False) or analysis.get("query_type") == "jd_info":
            return "jd_info"

        if tool_action:
            needs_search = tool_action.get("needs_candidate_search", False)

            if needs_search:
                # ── Optimisation 1: pending tool action (user is filling missing fields) ──
                # Candidates are already saved in pending_tool_action; skip search entirely.
                if conversation_context.get("pending_tool_action", {}).get("server_id") == tool_action.get("server_id"):
                    print("   ⚡ Skipping search: resuming pending tool action (fields already collected)")
                    return "execute_mcp_tool"

                # ── Optimisation 2: direct email supplied in query ──
                # No need to search the DB — the target is explicit.
                _query = state.get("query", "")
                if _re.search(r'\b[A-Za-z0-9._%+\-]+@[A-Za-z0-9.\-]+\.[A-Za-z]{2,}\b', _query):
                    print("   ⚡ Skipping search: direct email detected in query")
                    return "execute_mcp_tool"

                return "sql_filter"  # Normal candidate-search path
            else:
                print("   ⚡ Skipping search: tool doesn't need candidates")
                return "execute_mcp_tool"

        # Q&A query: use context
        if analysis.get("is_qa_query", False):
            return "fetch_context_candidates"

        return "sql_filter"
    
    workflow.add_conditional_edges(
        "route_to_next_jump",
        route_after_analysis,
        {
            "jd_resume_match": "jd_resume_match",
            "jd_info": "jd_info",
            "sql_filter": "sql_filter",
            "fetch_context_candidates": "fetch_context_candidates",
            "execute_mcp_tool": "execute_mcp_tool",
            "generate_answer": "generate_answer"
        }
    )

    workflow.add_edge("jd_resume_match", "generate_answer")
    workflow.add_edge("jd_info", "generate_answer")
    
    # Q&A queries: fetch context → generate answer
    workflow.add_edge("fetch_context_candidates", "generate_answer")
    
    workflow.add_edge("sql_filter", "llm_sql_generation")  # Try LLM SQL if needed
    workflow.add_edge("llm_sql_generation", "vector_search")
    workflow.add_edge("vector_search", "enrich_results")
    
    # After enriching results: run MCP tool if requested, else generate answer
    def route_after_enrich(state: AgentState) -> Literal["execute_mcp_tool", "generate_answer"]:
        """Route to MCP tool execution if a tool action is pending, else generate answer."""
        if state.get("tool_action") and not state.get("tool_executed", False):
            return "execute_mcp_tool"
        return "generate_answer"

    workflow.add_conditional_edges(
        "enrich_results",
        route_after_enrich,
        {"execute_mcp_tool": "execute_mcp_tool", "generate_answer": "generate_answer"}
    )

    # Tool executed → END (answer already set in execute_mcp_tool_node)
    workflow.add_edge("execute_mcp_tool", END)

    # Conditional retry logic
    workflow.add_conditional_edges(
        "generate_answer",
        should_retry_node,
        {"retry": "sql_filter", "end": END},  # Go back and try different strategy
    )

    return workflow.compile()


# ============= Easy-to-Use Interface =============


class ResumeIntelligenceAgent:
    """
    Main agent interface - handles everything automatically

    Usage:
        agent = ResumeIntelligenceAgent()
        answer = agent.query("Find Python developers with 5+ years")
    """

    def __init__(self):
        self.graph = create_intelligent_agent()

    def query(
        self, user_query: str, session_id: str = None, verbose: bool = True,
        conversation_context: dict = None
    ) -> dict:
        """
        Process a query and return natural language answer

        The agent will automatically:
        - Load conversation history (if session_id provided)
        - Save user message to database
        - Analyze the query to understand intent
        - Choose the best search strategy (SQL, vector, or hybrid)
        - Execute the search with appropriate filters
        - Deduplicate results
        - Generate a helpful answer
        - Save agent response to database
        - Retry with different strategy if needed

        Args:
            user_query: The user's question
            session_id: Optional session ID to continue a conversation
            verbose: Whether to print debug information

        Returns:
            Dict with keys: 'answer', 'session_id', 'candidate_ids'
        """
        # Import chat manager
        from chat.chat_manager import (
            create_chat_session,
            save_user_message,
            save_agent_message,
            load_chat_history,
        )

        # Create or use existing session
        if not session_id:
            session_id = create_chat_session(
                title=user_query
            )  # Use first 50 chars as title
            chat_history = []
        else:
            chat_history = load_chat_history(session_id, limit=10)

        # Save user message
        user_msg_id = save_user_message(session_id, user_query)

        if verbose:
            print("=" * 70)
            print(f"📝 USER QUERY: {user_query}")
            if chat_history:
                print(
                    f"💬 CONTINUING SESSION: {session_id} ({len(chat_history)} previous messages)"
                )
            else:
                print(f"🆕 NEW SESSION: {session_id}")
            print("=" * 70)

        # Initialize state with chat history
        initial_state = {
            "query": user_query,
            "query_analysis": {},
            "search_strategy": "hybrid",
            "sql_filters": {},
            "vector_query": user_query,
            "candidate_ids": [],
            "search_results": [],
            "final_results": [],
            "answer": "",
            "should_retry": False,
            "retry_count": 0,
            "use_llm_sql": False,
            "llm_generated_sql": "",
            "aggregation_result": 0,
            "chat_history": chat_history,  # ✅ Load from database
            "conversation_context": conversation_context or {},  # ✅ Persist across turns
            "tool_action": {},
            "tool_executed": False,
            "selected_jd": {},
            "jd_match_results": [],
        }

        # Run the graph
        final_state = self.graph.invoke(initial_state)

        # Extract results
        answer = final_state["answer"]
        final_results = final_state.get("final_results", [])
        candidate_ids = [result.get("resume_id") for result in final_results]
        candidate_names = [result.get("candidate_name") for result in final_results]  # ✅ NEW
        conversation_context = final_state.get("conversation_context", {})

        # Clarification turns often do not populate final_results, but we still
        # want the candidate context persisted with the session for the next turn.
        if not candidate_ids and isinstance(conversation_context, dict):
            pending = conversation_context.get("pending_clarification", {})
            if isinstance(pending, dict):
                candidate_ids = pending.get("candidate_ids", []) or conversation_context.get("candidate_ids", [])
                candidate_names = pending.get("candidate_names", []) or conversation_context.get("candidate_names", [])

        query_analysis = final_state.get("query_analysis", {})
        search_strategy = final_state.get("search_strategy", "unknown")

        # Save agent message with names AND IDs
        agent_msg_id = save_agent_message(
            session_id=session_id,
            content=answer,
            candidate_ids=candidate_ids,
            candidate_names=candidate_names,  # ✅ NEW
            search_type=search_strategy,
            query_analysis=query_analysis,
            conversation_context=conversation_context,
        )

        if verbose:
            print("\n" + "=" * 70)
            print("✅ FINAL ANSWER:")
            print("=" * 70)
            print(answer)
            print("=" * 70)

        return {
            "answer": answer,
            "session_id": session_id,
            "candidate_ids": candidate_ids,
            "conversation_context": conversation_context,
            "query_analysis": query_analysis,  # Exposed for interactive/debug scripts
        }


# ============= Testing =============

if __name__ == "__main__":
    agent = ResumeIntelligenceAgent()

    print("\n🤖 CONVERSATIONAL AGENT TEST\n")

    # First query - creates new session
    print("━" * 70)
    print("TEST 1: New conversation")
    print("━" * 70)
    result1 = agent.query("Find Python developers with more than 5 years experience")
    session_id = result1["session_id"]

    print(f"\n📊 Result: Found {len(result1['candidate_ids'])} candidates")
    print(f"🆔 Session ID: {session_id}")

    # Follow-up query - uses context
    print("\n" + "━" * 70)
    print("TEST 2: Follow-up question (should understand 'their')")
    print("━" * 70)
    result2 = agent.query("Show me their projects", session_id=session_id)

    print(f"\n📊 Result: {len(result2['candidate_ids'])} candidates")

    # Another follow-up
    print("\n" + "━" * 70)
    print("TEST 3: Another follow-up (should use same candidates)")
    print("━" * 70)
    result3 = agent.query("What are their skills?", session_id=session_id)

    print(f"\n📊 Result: {len(result3['candidate_ids'])} candidates")

    print("\n✅ CONVERSATIONAL TEST COMPLETE!")
